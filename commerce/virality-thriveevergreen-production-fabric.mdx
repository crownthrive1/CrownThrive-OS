---
title: "Virality Music × ThriveEvergreen Production Fabric"
sidebarTitle: "Virality × ThriveEvergreen"
description: "Current production truth for the Virality Music hybrid-commerce, wallet, product, provider, automation, and exact ECAC integration."
icon: "music"
---

# Virality Music × ThriveEvergreen Production Fabric

**Control date:** August 24, 2026

**Virality Music:** [vm.crownthrive.com](https://vm.crownthrive.com)

**Canonical engine:** `ct.agent.thriveevergreen-autonomous-publisher.v2`
**Overall commerce decision:** **HOLD**

<Warning>
  ThriveEvergreen may optimize within authority. It may never manufacture
  authority. Provider success is evidence, not CrownThrive economic truth.
</Warning>

## Production result

The internal ThriveEvergreen v2 publisher is active in
`production_write` with `publication_activation_state = ECAC`.
`D3` remains human-reserved. The separately governed generalized
`thriveevergreen.publish.dispatch` tool is disabled because its external
mutation contract is not certified.

The public Virality commerce control is live as version **1.1.1**. It is a
read-only control and deterministic denomination endpoint. It now:

- rejects a browser origin other than `https://vm.crownthrive.com` with 403;
- returns complete GET/POST/OPTIONS CORS headers only to the exact origin;
- limits request bodies to 4 KiB and accepts closed request shapes;
- rejects unsafe integer multiplication;
- reports the current 389-record native catalog rather than a stale count;
- preserves all 14 required schedules without creating duplicates;
- reports provider, wallet, site, and dispatch HOLD states without activating
  checkout, top-ups, signing, entitlements, settlement, rights, or publication;
- hard-codes checkout, Crown Credits top-up, native Sites mutation, Trust Wallet
  execution, and generalized dispatch authority to false until their exact ECAC
  and independent certification chains exist;
- reports the complete 58-tool VM/Fabric scope while keeping raw registry flags
  distinct from active/staged/disabled semantic classification;
- converts presentation units at exactly **1 Virality Credit = 25 Crown
  Credits** without mutating a ledger or rewriting a historical balance.

## Product truth

| Classification | Count | Meaning |
|---|---:|---|
| AVAILABLE | 2 | Exact zero-price public resources with deployed byte hashes; no economic authority is involved |
| HOLD | 385 | A price, version, rights, provider/security, native ledger, fulfillment, or exact ECAC gate remains |
| DENY | 2 | One retired record and one blocked record |
| Total | 389 | Current Virality native D1 catalog |

The populated Virality catalog was preserved. It was not replaced by the empty
central Sites projection.

The deployed source registry contains 355 of the 389 records. It has 353 paid
preparation records and two exact free resources. All 353 paid records remain
economically unavailable. Thirty-four D1-only records remain either HOLD or
DENY. Exact active D1 product-version evidence exists for 329 records.

Every row is reconciled in
`developers/manifests/virality-thriveevergreen-product-reconciliation.2026-08-24.v1.json`
with SKU, source version, content evidence when available, cash and
Crown/Virality Credit representation, rights, provider aliases, entitlement,
fulfillment, refund/dispute, settlement, and exact decision reasons.

## Attached master packet

The supplied source packet contains 16 files across 13 title groups:

- 14 PDFs and two DOCX editable sources;
- 294,208,601 total bytes;
- 2,908 PDF pages;
- 16 integrity/read passes;
- **AVAILABLE 0 · HOLD 16 · DENY 0**.

The files remain HOLD because no attachment carries the complete issued
SKU/offer/price/credits/license/entitlement/fulfillment/refund/settlement/provider
mapping required for commerce. The two *What Really Happened* PDFs are
conflicting manuscripts and must both be preserved until an external canonical
manifest selects an exact SHA. The two PDF/DOCX pairs are content-equivalent
source pairs, not byte-identical substitutes.

See
`developers/manifests/virality-attached-masters-reconciliation.2026-08-24.v1.json`
for exact filenames, SHA-256 values, byte/page counts, rights observations,
accessibility/print findings, source-pair evidence, and blockers.

## Hybrid commerce and wallet state

| Rail | Current truth | Effective action |
|---|---|---|
| Crown Credits | Canonical closed-loop program is active; native VM canonical-ledger binding is not | HOLD paid VM redemption |
| Virality Credits | Active presentation denomination at 25 Crown Credits per unit | Display/convert only; no historical rewrite |
| Stripe Direct | Provider account is configured | HOLD under independent security review |
| Stripe Connect | Existing target/provider connection is preserved | HOLD new OAuth, payout, earnings, settlement, refund, and dispute authority |
| Stripe stablecoin | Founder dashboard attestation exists; latest customer-effective checkout readback is HOLD and crypto was not offered | HOLD |
| MetaMask Embedded Wallet | Sapphire Mainnet and wallet services configured; origin is founder-attested | HOLD native injection and Add Funds at plan gate |
| WalletConnect | User self-custody with modal approval | HOLD until native injection; autonomous execution false; ceiling zero |
| Dedicated Agent Wallet | Bounded Trust Wallet Agent Kit mode is configured | HOLD runner/wallet/signing; unattended ceiling zero |
| Trust Wallet hosted plane | Hosted production intelligence is configured | Provider execution false; local signing plane absent |
| Changelly widget | Widget configuration read-verified with no revenue-share mode | Separate corridor; signed v2 HOLD for missing vaulted RSA key |

No Stripe, Connect, wallet, on-ramp, quote, transaction, webhook, redirect,
download, or provider fulfillment event can independently create credits,
entitlement, license, invoice, earnings, settlement, price, publication, public
performance, or governance authority.

## API and MCP state

The relevant live registry has 58 tools:

- **11 active** read/evaluation/advisory tools;
- **6 staged** wallet/runner tools, of which only the Agent Wallet exact policy
  evaluator has an enabled registry flag;
- **41 disabled** tools, including generalized publish dispatch, Crown Credits
  purchase/top-up, Stripe provider readback tools, Sites mutation, Trust Wallet
  provider/signing actions, Changelly controls, and uncertified Virality
  asset/statistics/commerce projections.

An enabled registry flag is not economic or execution authority. The advisory
anomaly, demand, and price models cannot issue ECAC, grant rights, publish an
effective price, mutate a provider, move money, create a payout, activate an
entitlement, or cast a vote.

## Automation preserved

The existing schedules remain active and were not duplicated:

- 10 bounded v2 publisher slots each hour at minutes 2, 8, 14, 20, 26, 32, 38,
  44, 50, and 56;
- Crown Credits Stripe reconciliation every 10 minutes;
- Stripe stablecoin effective-capability reconciliation at minute 17 every six
  hours;
- daily Fabric drift scan at 04:23 UTC;
- weekly advisory-ML calibration review at 05:41 UTC each Monday.

The most recent observed publisher slots, Crown Credits reconciliation,
stablecoin reconciliation, and weekly ML schedule succeeded. The daily drift
schedule has not yet recorded a scheduled execution, but its exact bootstrap
receipt passed against source anchor
`402ae2e2f35536adf0cca1979594067c8f7ed7e6`.

## Runtime hardening

The immutable v1 source packet remains byte-identical. Additive runtime v1.1
closes eight fail-open or ambiguous cases:

1. invalid candidate objects;
2. empty gate profiles;
3. non-canonical gate-object ordering;
4. lossy SKU collisions;
5. negative price or provider fee inputs;
6. empty provider expectations or malformed evidence digests;
7. revoked entitlement reactivation;
8. absent expected settlement values.

All eight negative vectors and eight positive controls pass. Runtime v1.1 still
produces only an external `ECAC_CANDIDATE`; every provider, price,
entitlement, rights, settlement, money, and publication effect remains false.

## Security and QA

Verified controls include:

- exact-origin CORS and explicit wrong-origin rejection;
- closed payload shapes, malformed JSON, payload size, and arithmetic overflow;
- unauthenticated and malformed-token fail-closed behavior on protected
  controls;
- structural idempotency uniqueness with no duplicate publisher attempts;
- D3 reservation and zero unattended wallet value;
- no raw-secret, Vault material, service-role key, or private-wallet export;
- RLS plus actual role grants on the publisher, DAIL, MCP, and Sites-control
  tables;
- read-after-write equality for the deployed Edge source;
- captured prior Edge sources as rollback/compensation payloads;
- a verified DAIL hash chain.

The Supabase Security Advisor currently reports 310 INFO
`rls_enabled_no_policy` notices. This label alone is not treated as proof of
safety. Actual `anon` and `authenticated` grants on the inspected control
tables are absent; publisher and DAIL tables force RLS. The performance advisor
reports 620 INFO notices: 289 unindexed foreign keys, 330 unused indexes, and
one connection-capacity notice.

## Native Sites gate

The public VM site and its catalog remain live. Production version 235 and its
deployed source/archive evidence are preserved.

No native production version was saved or deployed in this release. A
fail-closed source candidate is preserved on isolated branch
`codex/thriveevergreen-hybrid-commerce-hold-20260824` at exact commit
`74976d22ffc04ec8071533ad9d5ce1b0d292353c`. It adds a read-only account status
projection for the governed rails, holds new Stripe Connect onboarding, and
requires the exact canonical VM origin for economic, provider, entitlement,
license, and protected-file POSTs. Its remote branch SHA was read back exactly.

The candidate passed 137 commerce tests, six accessibility tests, the verified
build, Sites artifact validation, and diff checks. Those source results do not
activate the frontend. `ct.adapter.sites.virality.v1` still lacks certified
provider read, save/write canary, version read-after-write, production
regression, rollback proof, and independent certification. Production version
235 therefore remains unchanged and the frontend is not described as
wallet-integrated.

## Exact remaining blockers

- Native VM wallet/account UI injection and the Sites adapter are uncertified.
- Paid native redemption is not bound to the canonical Crown Credits ledger or
  an exact ThriveEvergreen ECAC receipt.
- Crown Credits, Stripe production, and Trust Wallet controls remain under
  independent security HOLD.
- Stripe stablecoin is not customer-effective in live Checkout.
- MetaMask origin state is founder-attested, not provider-machine readback;
  Add Funds remains at the plan gate.
- Agent Wallet persistent runner, encrypted institutional wallet, and signing
  path are absent; unattended value remains zero.
- Changelly signed v2 lacks the required vaulted RSA private key.
- Generalized publish dispatch and native Sites mutation contracts are not
  independently certified.
- The latest exact v2 candidate is HOLD with 12 blockers, 0/13 certifications,
  and 0/9 health receipts.
- Latest DAIL events remain externally unanchored even though the internal hash
  chain verifies.

## Evidence

- Product reconciliation:
  `developers/manifests/virality-thriveevergreen-product-reconciliation.2026-08-24.v1.json`
- Attached masters:
  `developers/manifests/virality-attached-masters-reconciliation.2026-08-24.v1.json`
- Additive runtime:
  `developers/scripts/thriveevergreen/production-fabric-runtime.v1.1.ts`
- Runtime contract:
  `developers/contracts/thriveevergreen/production-fabric.contracts.v1.1.schema.json`
- Edge control source:
  `developers/supabase/functions/virality-commerce-control/index.ts`
- Machine acceptance:
  `developers/certification/virality-thriveevergreen-production-integration-2026-08-24.v1.json`
