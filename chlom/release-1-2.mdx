---
title: "CHLOM Application 1.2 Record"
sidebarTitle: "Application 1.2"
description: "The bounded application 1.2 wallet and installation record, preserved separately from CHLOM core completion."
icon: "wallet"
standard_version: "1.0.0"
primary_audience: "developer"
page_type: "reference"
content_state: "current_with_holds"
---

{/* pentadocs:audience-orientation:v2 */}
<Note>
  **Audience:** builders and integrators (`developer`).

  **This page:** CHLOM Application 1.2 Record — The bounded application 1.2 wallet and installation record, preserved separately from CHLOM core completion.

  **Documentation state:** `current_with_holds` for page type `reference`. Documentation state is editorial posture only; it does not certify runtime, deployment, legal, rights, financial, provider, production, release, security, or operational status.

  **Related guidance:** [Developer platform](/developers/overview) and [API integration standards](/technology/api-integration-standards).
</Note>
{/* /pentadocs:audience-orientation:v2 */}

## Release scope

**Scope correction — September 20, 2026:** the founder's release target is CHLOM itself. LEX is a consuming application, and the application 1.2 delivery below is not a completion record for the entire protocol. See [CHLOM Core 1.3](/chlom/core-1-3) for the current implementation work. Preserve the dated readback below as predecessor evidence; a later deployment can replace the same public alias.

CHLOM is CrownThrive's Compliance Hybrid Licensing and Ownership Model for Rights, Rules, Roles, Revenue, Records and Remedies. CrownThrive operates within **Phase 3 — Execute**; component release versions and maturity are separate from that institutional phase.

**Application 1.2 deployment record; governed services reported configuration holds.** The reviewed implementation was merged in [protocol PR #51](https://github.com/crownthrive1/chlom-protocol/pull/51) at [source revision 6dc789e](https://github.com/crownthrive1/chlom-protocol/commit/6dc789ee334a46f4982e8fdd449494182fc8c22c). The [release-page alias](https://chlom-protocol.vercel.app/release) and public status endpoints were read back on September 20, 2026 at 02:17:25 UTC for deployment `dpl_FsNBhcpZqfbSdxiwpfMD1ijLhPkG`. This observation establishes that application deployment and its reported capability state; it does not establish customer fulfillment or activated economic services.

| Production observation | Readback |
|---|---|
| `/api/health` | HTTP 200; version `1.2.0`; source `6dc789ee334a46f4982e8fdd449494182fc8c22c`. |
| Governance readiness | `CONFIGURATION_HOLD`; API token and RPC configuration remain absent; write activation remains disabled. |
| `/api/wallet?route=status` | HTTP 200; `proof_verification: false`. Wallet message-proof verification is not enabled in this deployment. |
| `/api/protocol` | HTTP 200; policy and utility registries are unconfigured. |
| `/release` | Intended release page rendered in a browser. |

Installation artifacts, applied migrations, a real wallet journey, provider execution and recovery each require their own acceptance evidence. Native CHLOM LEX at chlomlex.com is a separate application.

## Wallet integration

The implementation adds injected EVM wallet discovery with EIP-6963/EIP-1193, connection and account/network change handling, and a server-verifiable signed-message observation when its server secret is configured. The deployed verifier currently reports disabled. The observation is limited to the browser flow; it does not authenticate an application account or establish a durable rights binding. Its stateless challenge can reproduce the same observation until expiry, so it is not a one-use authentication nonce. A real wallet-extension acceptance journey remains separate from the endpoint and page readback.

Wallet integration is a sequence of separately evidenced capabilities:

| Capability | What acceptance establishes |
|---|---|
| Browser provider discovery | The application can find and select an injected EVM provider using the supported discovery interface. |
| Account connection | The user permits the selected wallet to expose an address and network to the application. |
| Signature observation | A domain-bound, expiring challenge signature is verified for the stated address and scope. |
| Account authentication | A separate replay-safe identity/session contract binds the wallet to an authorized account. |
| Rights projection | Canonical CHLOM records establish licenses, entitlements, obligations, proofs and receipts for that account. |
| Economic execution | A separate authorized transaction produces verified provider receipts and reconciliation. |

A connected address does not grant asset ownership, copyright, a license, a governance vote or permission to spend funds. Signature observation must not be described as account authentication unless the complete session and replay controls are implemented and accepted. Contract-wallet verification, relay connections and embedded wallets must be declared separately when supported.

The complete CHLOM Wallet/Vault design covers ownership, licenses, credentials, entitlements, obligations, proofs, receipts, royalties and settlement status. Canonical rights and evidence must survive changes to wallet vendors, chains and hosting providers.

## Policy and proof boundaries

Adaptive compliance uses approved, versioned policy packs with source provenance, purpose, effective dates and explainable evaluation. AI can support classification, anomaly detection and review routing. It cannot silently enact a new policy or manufacture a legal determination.

The public audit projection should expose only authorized summaries and cryptographic commitments. Private evidence remains private by default. A message signature proves its stated signing claim; a hash commits to bytes; a Merkle proof establishes inclusion relative to a root. None of those should be labeled a zero-knowledge proof. ZK claims require the actual proof system, circuit/version, key custody and verifier acceptance.

## Token terminology and licensing

Historical papers describe CHLOM Coin as utility and CHM as governance; an experimental launch guide uses C-CHLOM and L-CHLOM. The current [source reconciliation](https://github.com/crownthrive1/chlom-protocol/blob/main/docs/architecture/SOURCE_RECONCILIATION.md) leaves final names and roles unresolved. Utility accounting, governance authority, transferable tokens and issuance remain distinct implementation choices.

The [repository license](https://github.com/crownthrive1/chlom-protocol/blob/main/LICENSE) is proprietary. Public source visibility does not grant an open-source or general commercial-use license. Separately licensed dependencies keep their own terms.

## Installation and acceptance

Use the [Node 24 and container installation guide](https://github.com/crownthrive1/chlom-protocol/blob/6dc789ee334a46f4982e8fdd449494182fc8c22c/docs/INSTALLATION.md), [wallet integration contract](https://github.com/crownthrive1/chlom-protocol/blob/6dc789ee334a46f4982e8fdd449494182fc8c22c/docs/WALLET_INTEGRATION.md), and [isolated database package guide](https://github.com/crownthrive1/chlom-protocol/blob/6dc789ee334a46f4982e8fdd449494182fc8c22c/docs/DATABASE_INSTALLATION.md) from this exact revision. The isolated database package requires provisioning and verification in an authorized Supabase project; its source publication is not evidence that a database migration was applied. Node/runtime support, package license, third-party notices and configuration requirements are part of the install contract.

Install from an identified release artifact or exact source revision. Use that release's documented runtime, dependency lock, configuration reference and migration instructions. Keep server credentials out of browser bundles and public documentation.

Acceptance should record:

1. Exact source version and artifact digest.
2. Reproducible build and relevant tests.
3. Installed runtime/configuration and applied migrations, if any.
4. Actual wallet selection, network/account changes, rejected signatures, expired challenges and disconnect behavior.
5. Exact deployment readback and recovery procedure.
6. Independent entitlement, payment or issuance acceptance for any commercial capability claimed.

A protocol installation is separate from native CHLOM LEX administrator setup. Published offers, price references or finite billing instructions do not establish customer authorization, an executed schedule, settled payment, delivery or payout.

## Ecosystem integration

CHLOM supplies portable rights and evidence to PentaGreen commerce, Go Flipbooks publishing, Virality Music and CrownThrive Studios, Locticians and Melanated media, AdLuxe advertising, CrownThriveU education and CrownThrive IO integrations. Each platform binds its own exact business object, license, entitlement and provider receipt. A common wallet view does not replace those records.

See the [September 20 source reconciliation](https://github.com/crownthrive1/chlom-protocol/blob/main/docs/architecture/CHLOM_RELEASE_SOURCE_RECONCILIATION_20260920.md), [cryptographic substrate](https://github.com/crownthrive1/chlom-protocol/blob/main/docs/architecture/CHLOM_CRYPTOGRAPHIC_SUBSTRATE_V1.md) and [historical tokenomics](/chlom/historical-tokenomics-chlom-coin-chm).

## Support and correction

For installation or licensing support, contact [CrownThrive support](mailto:contact@crownthrive.com). Report the release/source identifier, affected route, expected result and sanitized error. Never send wallet seed phrases or private keys. Corrections should append the new observation and preserve its predecessor; source acceptance, rendered publication and runtime acceptance stay separate.
