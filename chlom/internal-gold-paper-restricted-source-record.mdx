---
title: "CHLOM Internal Gold Paper — Restricted Source Record"
sidebarTitle: "Internal Gold Paper"
description: "Public-safe source record for the recovered CHLOM Internal Gold Paper MTD v0.9-internal, preserving formal-proof, royalty-math, security, TLA+, governance and calibration lineage without publishing restricted implementation details."
icon: "lock"
noindex: true
keywords: ["CHLOM Internal Gold Paper", "Gold Paper", "Master Technical Document", "formal proof", "TLA+", "royalty math", "security", "restricted source"]
---

# CHLOM Internal Gold Paper — Restricted Source Record

A complete source body has been recovered for **CHLOM™ Internal Gold Paper — Master Technical Document (MTD)**.

The original file explicitly marks itself **Confidential – Internal Use Only** and says external sharing requires written Founder approval. This Mintlify page therefore records **public-safe provenance, mathematical lineage and architectural significance** without reproducing proprietary implementation details that the source classifies as restricted.

## Source envelope

```yaml
exact_title: CHLOM™ Internal Gold Paper — Master Technical Document (MTD)
version: v0.9-internal
date: 2025-08-10
owner: CrownThrive LLC
maintainers: Governance Scribes
source_recovery: complete_source_body_located
source_pages: 10
source_classification: confidential_internal
mintlify_projection: public_safe_metadata_and_lineage_only
historical_patent_pending_text: preserved_as_historical_only
current_patent_pending_claim: false_unless_future_verified_filing
current_authority: historical_deep_design_source
founder_adjudication: open_after_publication
```

## What the recovered Gold Paper is

The source defines a formal model for what it would mean to say CHLOM "works." It then organizes mathematical/security guarantees around that model.

Its scope includes:

- protocol architecture;
- identity/DID and private Fingerprint lineage;
- license lifecycle;
- royalty/splitter mathematics;
- override trees;
- treasury and slashing;
- governance;
- zero-knowledge verification;
- oracles;
- cross-chain settlement;
- indexers/audits;
- security and threat assumptions;
- developer environment;
- deployment sequence;
- SDK/API surface;
- KPIs;
- Compliance Packs;
- runbooks;
- SLA/versioning;
- formal proof corpus;
- TLA\+ starter specification;
- economic calibration worksheets.

## Public-safe architecture reconstruction

The source's internal ASCII architecture can be summarized without exposing restricted Fingerprint or calibration implementation:

```text
DID / IDENTITY
     |
     +------------------+
     |                  |
     v                  v
LICENSE ENGINE      GOVERNANCE / REVIEW
     |                  |
     v                  v
ZK VERIFICATION <--> RISK / ORACLE INPUT
     |
     v
ROYALTY / OVERRIDE LOGIC
     |
     v
TREASURY / SETTLEMENT
     |
     v
AUDIT / INDEXER / RECEIPTS
     |
     v
OPTIONAL CROSS-CHAIN PROOFS
```

The original implementation details remain restricted.

## Recovered formal-guarantee families

The source identifies fourteen high-level guarantee families:

```text
P1  Value conservation
P2  Capacity safety
P3  Attribution uniqueness
P4  Privacy-preserving compliance
P5  Eventual enforcement
P6  Incentive compatibility
P7  Audit immutability
P8  Compositionality
P9  Batch invariance
P10 Subtree-revocation conservation
P11 Rounding bound
P12 Collusion thresholds
P13 Oracle accountability
P14 Cross-chain settlement safety
```

The source appendix further numbers **Theorems 1–15** implementing these proof families.

## Public-safe theorem register

The recovered theorem topics are:

| Theorem | Source topic | Public-safe institutional mapping |
| --- | --- | --- |
| 1 | Value Conservation — Splitter | economic conservation invariant |
| 2 | Override Correctness — Tree | hierarchical split/right routing |
| 3 | Capacity Safety | usage/cap state-machine safety |
| 4 | Attribution Uniqueness — DID | identity/key collision assumptions |
| 5 | ZK Compliance | proof verification implies circuit predicate under assumptions |
| 6 | Eventual Enforcement | liveness/finality assumption |
| 7 | Incentive Compatibility | stake/detection/slash calibration |
| 8 | Audit Immutability | finalized event integrity |
| 9 | Compositional Compliance | conjunction of accepted predicates |
| 10 | Batch Invariance | allocation totals independent of partition/order |
| 11 | Subtree Revocation | no future flow to revoked subtree while preserving conservation |
| 12 | Rounding Bound | bounded deterministic residual |
| 13 | Collusion Thresholds | BFT safety/censorship assumptions |
| 14 | Oracle Accountability | bonded false-report deterrence |
| 15 | Cross-Chain Safety | light-client/finality proof transfer assumptions |

These are **historical formal-design claims under explicit assumptions**. This record does not certify that each theorem has been independently peer reviewed, mechanically verified or deployed in current CHLOM.

## Royalty mathematics recovered

The Gold Paper contains a concrete splitter model.

Let:

```text
D = denominator
B = base amount
weights = [a1, a2, ..., am]
Σ ai = D
```

Then source payout logic is:

```text
p_i = floor(a_i * B / D)
```

Residual:

```text
r = B - Σ p_i
```

The source states a bounded residual relationship:

```text
0 <= r <= m - 1
```

with a deterministic residual sink.

### Current mapping

Current cloud-first CHLOM can preserve this deterministic allocation principle without using tokens or on-chain contracts:

```text
versioned economic schedule
       |
       v
integer-safe allocation
       |
       v
deterministic rounding residue
       |
       v
allocation receipt
       |
       v
provider execution + reconciliation
       |
       v
DAIL
```

## Override-tree mathematics

The source uses an override tree where an ancestor share is based on the product of edge rates along a path.

Public-safe form:

```text
ancestor_share = B * product(r_e for each edge e on path)
```

The source claims tree totals remain value-conserving under its model.

Current implementation must additionally require that economic routing does not exceed legal/contractual rights.

## Incentive-calibration equation

A historically recovered deterrence relationship is:

```text
S >= G / (p * s)
```

Where, in the source model:

- `S` = required stake/bond;
- `G` = potential gain from misconduct;
- `p` = detection probability;
- `s` = slash fraction/severity.

Equivalent deterrence condition:

```text
p * s * S >= G
```

This is historical mechanism-design research, not a current required bond or financial product.

## Oracle-accountability relationship

The source applies the same general logic to an oracle bond:

```text
p_det * s_V * S_V >= benefit_false
```

The intent is negative expected value for false reporting.

Current CHLOM can preserve the **assurance principle** without requiring CHM or a public token.

## BFT assumptions

The Gold Paper explicitly assumes a Byzantine-fault-tolerant environment with a threshold described as:

```text
f < n/3
```

and discusses historical collusion thresholds around one-third censorship and greater-than-two-thirds safety failure.

These assumptions belong to the historical decentralized-network architecture, not the current cloud-first service baseline.

## ZK template lineage

The restricted source identifies representative proof categories such as:

```text
age threshold
geographic allow-list membership
risk below threshold
```

Current documentation preserves only the public-safe proof categories and policy interfaces. Circuit internals remain implementation-specific.

## TLA\+ lineage

The source includes a starter TLA\+ sketch with conceptual variables around:

```text
license state
usage / cap
nonce
split vector
override tree
ledger
```

and transition families around usage advancement, payout, subtree revocation and ZK verification.

This establishes **formal-modeling IP lineage**. It does not claim the current repository contains a fully model-checked canonical TLA\+ specification unless separately verified.

## Security assumptions and threat families

Public-safe threat categories recovered include:

- MEV/censorship;
- double-use/replay;
- identity collision;
- false attestation;
- bridge risk;
- rounding drift;
- mass revocation;
- key compromise.

The detailed mitigations and security parameters remain subject to current security architecture and should not be copied mechanically from the old network design.

## Historical governance phases

The source describes a six-stage historical adoption path from development/testing through builder onboarding, validator/AI co-training, ecosystem rollout and eventual decentralization.

It also contains a historical Phase 1 reference to XENthrive transfer of docs/IP. **XENthrive is historical lineage, not a current primary CHLOM player.**

The 2026 Technical Build Pack supersedes this implementation sequencing for current work.

## Historical Fingerprint boundary

The source explicitly says public DID could be public while Fingerprint internals remain private.

Current institutional interpretation preserves the broader privacy principle:

```text
identity proof != public disclosure of protected identity telemetry
```

## Source-integrity warning

The Gold Paper contains historical `Patent Pending` language. Current institutional records have separately corrected that status: no current patent-pending claim should be made unless a verified future filing creates it.

Original text remains part of historical provenance.

## Restricted-IP rule

Do not publish from this source by default:

- proprietary Fingerprint internals;
- private calibration worksheets;
- exploit-sensitive security implementation;
- protected pseudocode beyond public-safe interfaces;
- restricted parameter registry;
- confidential partner/deployment detail.

## Paper-to-asset extraction

The source now feeds durable records for:

```text
Gold Paper source
 -> theorem/proof register
 -> formula registry
 -> algorithm registry
 -> TLA+ lineage
 -> royalty allocation model
 -> threat model
 -> ZK proof-family registry
 -> developer test vectors
 -> historical governance lineage
```

## Current authority boundary

```text
historical theorem != current certification
historical proof sketch != independent formal verification
historical token/bond equation != current economics
historical BFT model != current cloud architecture
historical private algorithm != public documentation
```

## Founder adjudication

`open_after_publication`

A Founder adjudication may designate specific Gold Paper concepts as current, rejected, retired, confidential, implementation candidates or superseded. The original source remains preserved.