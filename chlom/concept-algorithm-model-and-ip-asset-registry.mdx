---
title: "CHLOM Concept, Algorithm, Model and IP Asset Registry"
sidebarTitle: "Concept & IP Asset Registry"
description: "Controlled registry model for CHLOM concepts, algorithms, mathematical models, paper-derived inventions, diagrams, schemas, state machines, formulas and defensibility metadata."
icon: "vault"
keywords: ["CHLOM IP", "algorithm registry", "concept registry", "mathematical model", "pseudocode", "trade secrets", "defensibility", "paper decomposition"]
---

# CHLOM Concept, Algorithm, Model and IP Asset Registry

CHLOM's intellectual property is broader than paper files. A paper may contain dozens of independent concepts, algorithms, equations, diagrams, data schemas, workflow methods and system invariants. Those assets need **stable institutional identities** so they can be tracked, protected, implemented and superseded independently.

## Registry status

```yaml
article_state: reconstructed_source_grounded_plus_current_registry_design
current_role: technical_IP_asset_registry_contract
paper_is_asset_container: true
paper_equals_implementation: false
protected_IP_publication: metadata_and_public_safe_patterns_only
founder_adjudication: open_after_publication
```

## Asset hierarchy

```text
PAPER / SOURCE
    |
    +--> CONCEPT
    +--> ALGORITHM
    +--> FORMULA / MODEL
    +--> STATE MACHINE
    +--> SCHEMA
    +--> ASCII MODEL
    +--> POLICY PATTERN
    +--> INTERFACE CONTRACT
    +--> THREAT MODEL
    +--> TEST VECTOR
    +--> IMPLEMENTATION REQUIREMENT
```

## Core distinction

```text
paper_id != concept_id != algorithm_id != implementation_id
```

One concept may appear in many papers. One algorithm may implement multiple concepts. One implementation may replace another without changing the underlying institutional concept.

## Why this registry exists

Without asset-level records, CHLOM knowledge collapses into documents that are hard to compare, version or implement.

```text
DOCUMENT-CENTRIC ONLY
       |
       +--> same idea copied across papers
       +--> formulas buried in appendices
       +--> algorithms lose provenance
       +--> diagrams become screenshots
       +--> implementation status becomes ambiguous

ASSET REGISTRY
       |
       +--> stable concept ID
       +--> source provenance
       +--> current authority
       +--> version / status
       +--> implementation links
       +--> test / evidence links
       +--> confidentiality
```

## Asset class: Concept

An abstract institutional or technical idea such as:

- Rights-before-Revenue;
- Dynamic Licensing Asset;
- Decentralized Autonomous Information Ledger;
- Compliance Pack;
- Fingerprint identity;
- rights graph;
- Oracle Lattice;
- Smart Treasury;
- LEX;
- founder adjudication / correction without erasure.

### Concept record

```yaml
concept_id:
name:
definition:
source_refs: []
source_eras: []
current_meaning:
historical_meanings: []
status:
authority_class:
confidentiality:
predecessors: []
successors: []
related_concepts: []
implementation_refs: []
founder_adjudication_state:
```

## Asset class: Algorithm

A repeatable decision or transformation procedure such as:

- policy evaluation;
- rights closure;
- sublicensing validation;
- oracle aggregation;
- allocation and reconciliation;
- proof verification;
- DAIL correction propagation.

### Algorithm record

```yaml
algorithm_id:
name:
classification: recovered|reconstructed|derived
visibility: public_safe|internal|restricted|trade_secret
source_refs: []
inputs: []
outputs: []
preconditions: []
postconditions: []
invariants: []
side_effect_class:
authority_requirements: []
failure_states: []
idempotency:
rollback_compensation:
formula_refs: []
test_vector_refs: []
implementation_refs: []
version:
status:
```

## Asset class: Mathematical model

A formula or quantitative relation such as:

- net supply change;
- staking APY;
- basis-point allocation;
- quorum ratio;
- risk score;
- reserve ratio;
- SLA availability.

### Formula/model record

```yaml
formula_id:
name:
expression:
variables:
units:
source_ref:
classification: historical_source_formula|historical_source_example|derived_model|adopted_rule
assumptions: []
parameter_refs: []
rounding_policy:
sensitivity_refs: []
test_vectors: []
adopted: false
production_parameter_refs: []
```

## Asset class: State machine

A controlled set of states and transitions for DLA, LEX, credentials, policies, cases, payments, reconstruction or release.

```yaml
state_machine_id:
name:
states: []
transitions: []
transition_guards: []
invariants: []
terminal_states: []
correction_paths: []
source_refs: []
version:
```

## Asset class: Schema

A machine-readable object or event contract.

```yaml
schema_id:
name:
schema_type: entity|event|receipt|policy|API|manifest
version:
canonical_fields: []
required_fields: []
confidential_fields: []
compatibility_rules: []
source_refs: []
implementation_refs: []
```

## Asset class: ASCII model

A portable explanatory architecture diagram.

```yaml
model_id:
name:
source_type: source_recreated|modernized_mapping|derived_explanatory
source_refs: []
version:
concept_ids: []
algorithm_ids: []
confidentiality:
supersedes: []
```

ASCII is explanation, not executable state.

## Asset class: Threat model

```yaml
threat_model_id:
name:
assets: []
trust_boundaries: []
threat_actors: []
attack_paths: []
mitigations: []
residual_risks: []
monitoring_refs: []
incident_refs: []
version:
```

## Asset class: Test vector

```yaml
test_vector_id:
name:
classification: source_example|derived_conformance|regression|adversarial
algorithm_ref:
formula_ref:
inputs:
expected_intermediate:
expected_output:
expected_disposition:
rounding:
source_ref:
version:
```

## Stable ID namespace

Candidate institutional patterns:

```text
ct.chlom.concept.<slug>
ct.chlom.algorithm.<slug>
ct.chlom.formula.<slug>
ct.chlom.schema.<slug>
ct.chlom.state-machine.<slug>
ct.chlom.ascii.<slug>
ct.chlom.threat-model.<slug>
ct.chlom.test-vector.<slug>
```

Semantic version should normally be metadata so stable identity survives upgrades unless a later ADR requires version in the ID.

## Example concept record

```yaml
concept_id: ct.chlom.concept.rights-before-revenue
name: Rights Before Revenue
class: institutional_invariant
source_refs:
  - CHLOM Grand Prospectus
  - CHLOM LEX Concept
  - CHLOM-ARCH-003
current_authority: current_doctrine
public_safe: true
related:
  - Dynamic Licensing Asset
  - LEX
  - ThriveEvergreen
derived_algorithms:
  - ct.chlom.algorithm.lex-listing-eligibility
invariants:
  - payment_does_not_create_rights
```

## Example algorithm record

```yaml
algorithm_id: ct.chlom.algorithm.rights-closure
name: Effective Rights Closure
classification: reconstructed_public_safe
source_refs:
  - CHLOM-ARCH-003
  - Technical Blueprint
inputs:
  - source_rights
  - parent_grants
  - exclusions
  - territory
  - channels
  - term
output: effective_rights_set
invariants:
  - child_rights_subset_parent
visibility: public_safe_pattern
implementation_refs: []
```

## Example formula record

```yaml
formula_id: ct.chlom.formula.net-supply-change
name: Net Supply Change
expression: delta_S = i*S - b*N
origin: Genesis Volume Appendix D
classification: historical_source_formula
current_adoption: false
legal_economic_status: research_only
```

## Source-era labels

Each asset should preserve its source era:

```text
foundational_2025
prospectus_consolidation
technical_blueprint
help_center_legacy
build_pack_2026
current_reconstruction
runtime_implementation
```

## Authority classes for IP records

```text
historical_source
research
reconstructed_requirement
current_doctrine
current_technical_baseline
controlled_test
production_verified
superseded
```

Do not use `production_verified` without executable/runtime evidence.

## Confidentiality classes

```text
PUBLIC
INTERNAL
RESTRICTED
EVIDENCE_PRIVATE
STRATEGIC
TRADE_SECRET
```

### Public-safe disclosure rule

For protected algorithms:

```text
PUBLIC:
  purpose
  inputs/outputs
  safety invariants
  interface behavior
  broad failure modes

DO NOT PUBLICLY EXPOSE BY DEFAULT:
  proprietary weights
  exploit-sensitive heuristics
  keys / secrets
  security bypass logic
  private training data
  protected source code
```

## Defensibility tags

Possible metadata:

```yaml
trade_secret_relevance: high|medium|low|none
copyright_relevance: high|medium|low
trademark_relevance: false
patentability_review: not_reviewed|candidate|reviewed
prior_art_review: not_started
public_disclosure_risk: low|medium|high
reverse_engineering_risk: low|medium|high
```

Historical documents that used patent-pending language do not create current patent-pending status.

## Current concept families

```text
A. Rights / Licensing
   - rights graph
   - rights-before-revenue
   - Dynamic Licensing Asset
   - licensing stewardship
   - LEX

B. Rules / Compliance
   - Compliance Packs
   - Policy Engine
   - ACE
   - PASS/HOLD/DENY

C. Roles / Authority
   - stable identity
   - credential
   - role
   - delegation
   - approval
   - Scribe/verifier

D. Records / Evidence
   - DAIL
   - attestations
   - proof anchors
   - evidence vault
   - correction chains

E. Revenue
   - economic schedule
   - ADE attribution/distribution
   - royalty/splits
   - settlement/reconciliation
   - Smart Treasury lineage

F. Remedies
   - cases
   - disputes
   - guardianship
   - appeals
   - corrections/reversals
```

## Algorithm families currently registered conceptually

```text
A. Identity / Credential
   - identity resolution
   - credential verification
   - revocation / freshness
   - recovery / key rotation

B. Rights / Licensing
   - chain-of-title traversal
   - rights intersection
   - sublicensing validation
   - DLA lifecycle

C. Policy / Compliance
   - pack applicability
   - stack precedence
   - PASS/HOLD/DENY aggregation
   - policy migration / simulation

D. Evidence / DAIL
   - normalization
   - event append
   - correction propagation
   - public projection

E. Intelligence
   - risk scoring
   - anomaly detection
   - model drift
   - policy simulation

F. Oracle / Human Assurance
   - source aggregation
   - quorum
   - reviewer assignment
   - conflict removal

G. LEX / Commerce
   - listing eligibility
   - agreement/entitlement gating
   - payment idempotency
   - settlement reconciliation

H. Economics
   - royalty/split allocation
   - treasury waterfall
   - reserve rules
   - corrections/reversals

I. Privacy / Proof
   - proof verification
   - circuit/version validation
   - witness freshness

J. Operations
   - timelock execution
   - SLA monitoring
   - audit scheduler
   - release certification
```

## Concept dependency graph

```text
Identity
   |
   +--------+
            v
Rights --> Policy --> DLA --> LEX --> Entitlement
  |          |       |       |          |
  |          |       |       |          v
  |          |       |       +------> Revenue
  |          |       |                  |
  |          |       v                  v
  |          +----> Cases ----------> Remedies
  |                                      |
  +-------------------> Evidence --------+
                         |
                         v
                        DAIL
```

## Provenance graph

```text
SOURCE FILE
    |
    v
SOURCE HASH / REFERENCE
    |
    v
PAPER RECORD
    |
    v
CONCEPT / MODEL / ALGORITHM RECORD
    |
    v
REQUIREMENT
    |
    v
TEST
    |
    v
IMPLEMENTATION
    |
    v
RUNTIME EVIDENCE
```

## Novelty versus implementation

A novel concept can exist before software.

```text
concept exists != code exists
code exists != tested
 tested != deployed
 deployed != adopted institutionally
```

All are separate states.

## Reconstruction of missing algorithm bodies

If an old Help Center title says an algorithm existed but the body is missing:

```text
1. register title / concept lineage
2. locate source references
3. reconstruct only supported interface/invariants
4. label reconstruction explicitly
5. do not claim verbatim original algorithm
6. allow founder adjudication
7. link future implementation separately
```

## Paper-to-asset extraction checklist

For every substantive paper:

```text
[ ] concepts extracted
[ ] algorithms extracted
[ ] equations extracted
[ ] pseudocode extracted
[ ] schemas extracted
[ ] ASCII/figures indexed
[ ] state machines extracted
[ ] invariants extracted
[ ] threat models extracted
[ ] test vectors extracted
[ ] product requirements extracted
[ ] current/historical status mapped
[ ] confidentiality classified
[ ] founder conflicts flagged
```

## Algorithm change control

```text
algorithm v1
   |
   v
proposed v2
   |
   v
unit / property / regression tests
   |
   v
simulation / historical replay
   |
   v
security / bias / authority review
   |
   v
approval
   |
   v
controlled deployment
   |
   v
monitoring / rollback
```

## Formula change control

A formula version change must document:

- mathematical expression;
- variable meanings;
- units;
- input sources;
- changed assumptions;
- old/new output comparisons;
- threshold implications;
- affected economic/policy contracts;
- migration behavior.

## Concept collision handling

Acronym collision or concept reuse must never overwrite older IP records.

Example:

```text
AIE: AI Intelligence Engine
AIE: Anomaly Intelligence Engine
```

Represent as two concept records with a documented relationship rather than one mutable definition.

## Asset status state machine

```text
discovered
 -> source_grounded
 -> decomposed
 -> classified
 -> specified
 -> prototyped
 -> tested
 -> controlled_test
 -> production_verified
 -> maintained
 -> superseded
```

Any asset may stop at historical/research state indefinitely.

## Vault / custody expectation

Restricted source files, detailed algorithm bodies, proprietary research and machine manifests should have protected canonical custody outside public documentation, with public Mintlify pages providing safe indexes and interpretations.

## Founder adjudication

A Founder decision may:

- declare a concept canonical or retired;
- resolve duplicate algorithms;
- reclassify a formula as historical only;
- define color-paper semantics;
- set confidentiality;
- rename a component;
- approve a reconstructed model for implementation.

The decision appends a new effective interpretation. It does not destroy source lineage.

## Final institutional rule

If CHLOM creates an idea worth naming, a model worth calculating, an algorithm worth using, or a diagram worth preserving, it receives a durable record.

```text
IF IT MATTERS TO CHLOM
      |
      v
GIVE IT AN ID
      |
      v
GIVE IT PROVENANCE
      |
      v
GIVE IT A VERSION
      |
      v
GIVE IT A STATUS
      |
      v
GIVE IT TESTS / EVIDENCE WHEN IMPLEMENTED
```

That is how CHLOM technical IP stops being trapped inside documents and becomes governed institutional memory.