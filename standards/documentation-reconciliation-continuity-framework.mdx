---
title: "Documentation Reconciliation & Continuity Framework"
sidebarTitle: "Docs Reconciliation Framework"
description: "The two-pass CrownThrive framework for stale-state reconciliation, gap closure, continuity linking, Help Center article reconstruction and current Phase 3 execution evidence."
icon: "arrows-rotate"
keywords: ["documentation reconciliation", "stale docs", "gap closure", "internal links", "Help Center", "795 articles", "Phase 3", "PentaFactory"]
standard_version: "1.0.0"
primary_audience: "operator"
page_type: "standard"
content_state: "unresolved"
---

{/* pentadocs:audience-orientation:v2 */}
<Note>
  **Audience:** operators and administrators (`operator`).

  **This page:** Documentation Reconciliation &amp; Continuity Framework — The two-pass CrownThrive framework for stale-state reconciliation, gap closure, continuity linking, Help Center article reconstruction and current Phase 3 execution evidence.

  **Documentation state:** `unresolved` for page type `standard`. Documentation state is editorial posture only; it does not certify runtime, deployment, legal, rights, financial, provider, production, release, security, or operational status.

  **Unresolved reason:** no explicit page-level editorial acceptance state was declared in the migrated source.

  **Evidence needed:** an accepted governing source or accountable-owner attestation.

  **Review role:** PentaDocs governance.

  **Review trigger:** next material edit or source reconciliation.

  **Related guidance:** [Documentation source governance](/standards/documentation-source-of-truth-and-autonomous-governance) and [Institutional record standard](/standards/record-and-format-standard).
</Note>
{/* /pentadocs:audience-orientation:v2 */}

## Documentation Reconciliation & Continuity Framework

The **Documentation Reconciliation & Continuity Framework** is CrownThrive's governed operating framework for keeping institutional documentation current, complete, internally connected, recoverable and machine-addressable while the ecosystem continues to grow.

**Framework ID:** `ct.framework.documentation-reconciliation-continuity`<br />**Package ID:** `ct.framework-package.documentation-reconciliation-continuity`<br />**Version:** `1.0.0`<br />**Current state:** `controlled_test`<br />**Default authority ceiling:** `D2`<br />**D3 authority:** human-reserved<br />**Self-certification:** prohibited

<Columns cols={2}>
  <Card title="Pass A — Reconcile stale state" icon="clock-rotate-left" href="/standards/versioning-change-control-and-correction">
    Compare scoped pages against current canonical names, source authority, lifecycle, provider/API state, phase namespaces, legal/rights/economic boundaries and accepted adjudications.
  </Card>

  <Card title="Pass B — Close gaps & connect continuity" icon="link" href="/support/articleization-manifest-and-autopublication-contract">
    Fill evidence-backed omissions, add meaningful internal links, open explicit unresolved deltas and propagate article/PENTA impacts.
  </Card>
</Columns>

## Why this exists

CrownThrive's documentation estate is no longer a static help center. It represents institutional doctrine, platform intelligence, rights, commerce, governance, technical architecture, recovery history, current state, future gates and machine-readable handoff context. That means a page can become stale even when its prose remains grammatically correct.

A sprint therefore cannot stop after correcting old wording. It must also ask what the page still **does not say**, what accepted evidence it fails to reference, which other records a future operator would need, and how the page affects article reconstruction and the current Phase 3 execution gate.

## Mandatory two-pass sprint contract

### Pass A — stale-state reconciliation

For every page in scope:

1. freeze the baseline commit and page list;
2. identify stale canonical names, aliases, statuses, provider/version claims, phase references, counts, architecture assumptions and public claims;
3. resolve against the [Source Authority Hierarchy](/knowledge/source-authority-hierarchy), [Evidence, Claims and Proof Standard](/standards/evidence-claims-and-proof-standard), current registries and adopted adjudications;
4. correct only what the evidence supports;
5. preserve historical statements, literal provider identifiers and deployed legacy physical names when they remain factually necessary;
6. version material corrections under the [Versioning & Correction Standard](/standards/versioning-change-control-and-correction);
7. emit one inspectable page outcome: `docs_updated`, `docs_no_change` or `docs_delta_opened`.

### Pass B — gap closure and continuity

For the same page set:

1. identify missing source qualifiers, relationships, API/provider boundaries, legal/rights/support state, runbook references, successor/predecessor lineage, phase impacts, machine-seed requirements and article-rebuild dependencies;
2. add facts only when supported by accepted evidence;
3. add continuity links to the records a future operator or agent needs to reconstruct meaning;
4. convert unsupported assumptions into explicit unresolved records instead of prose guesses;
5. update the 795-title article-rebuild mapping where the page closes, merges or creates a documentation responsibility;
6. record current Phase 3 execution contribution and remaining blockers;
7. propagate relevant future-phase implications.

## Cross-surface documentation contract

The framework now treats documentation as one governed fabric across runtime, DAIL, structured ledgers, repository source, Drive, public PentaDocs, support and applicable product-publication destinations.

A claim that material is “documented everywhere” is valid only when the required destinations for that subject are known, updated and read back. Presence in one database table, one Google Doc, one repository file or one public page is insufficient.

```text
SOURCE PRESENT
≠ RECONCILED
≠ DOCUMENTED
≠ REVIEWED
≠ MERGED
≠ DEPLOYED
≠ PROVIDER READ BACK
≠ PUBLICLY CURRENT
≠ RUNTIME CERTIFIED
```

Documentation state remains separate from runtime, provider, rights, release and authority state.

### Canonical source hierarchy

Unless a system-specific contract states otherwise, resolve mutable claims in this order:

1. current provider or runtime readback;
2. canonical DAIL event and terminal receipt;
3. governed database ledger or registry;
4. accepted repository source and exact commit;
5. governed Drive source and phase record;
6. public PentaDocs or help-center projection after provider readback;
7. historical documents as lineage unless explicitly reaffirmed.

A lower source never silently overrides a higher current source. Conflicts must be preserved, classified and reconciled.

### Required surface classes

| Surface | Minimum institutional record |
| --- | --- |
| Runtime/provider | Current readback with observation boundary |
| DAIL | Material change, correction, release or terminal receipt |
| Structured ledgers | Stable identity, state, version and evidence hash |
| Repository | Exact branch, commit, review and merge state |
| Drive | Governed file ID, revision, folder and accountable title |
| Public documentation | Deployment state plus exact route readback |
| Product/publication | Exact package and destination readback where required |
| Support | Current guide, runbook, escalation path and owner |
| Master records | CHLOM, phase, DAIL, IP/asset and product ledgers as applicable |

A private architecture may intentionally omit a public or commerce destination. That omission must be recorded as `NOT_APPLICABLE` or a named `HOLD`, not left ambiguous.

### State vocabulary

Use these states consistently:

- `DISCOVERED`
- `SOURCE_PRESENT`
- `EXTRACTED`
- `CONFLICT`
- `RECONCILED`
- `DOCUMENTED`
- `REVIEWED`
- `MERGED`
- `DEPLOYED`
- `PROVIDER_READBACK`
- `PUBLIC_CURRENT`
- `SUPERSEDED`
- `RETIRED`
- named `HOLD_*`

Do not promote a hold by weakening the requirement or hiding missing evidence.

### Evidence envelope

A material documentation projection should resolve:

```json
{
  "stable_id": "...",
  "title": "...",
  "subject_type": "architecture|platform|product|release|runbook|standard|asset",
  "source_refs": ["..."],
  "source_authority": "runtime|DAIL|ledger|repository|Drive|historical",
  "source_version": "...",
  "content_sha256": "...",
  "documentation_state": "RECONCILED",
  "runtime_state": "separate value or null",
  "authority_state": "separate value or null",
  "repository_ref": "owner/repo@commit or PR",
  "drive_file_ids": ["..."],
  "public_routes": ["..."],
  "provider_readback": "PASS|HOLD|NOT_APPLICABLE",
  "conflicts": [],
  "supersedes": ["..."],
  "accountable_owner": "...",
  "observed_at": "...",
  "next_review_trigger": "..."
}
```

Evidence hashes should cover the intended material, not volatile observation timestamps unless the timestamp is itself part of the claim.

### Cross-surface release sequence

1. Resolve the subject identity and exact sources.
2. Read current mutable runtime/provider state where applicable.
3. Preserve the predecessor source, file revision, page version or ledger state.
4. Reconcile contradictions and authority boundaries.
5. Build equivalent human and machine projections.
6. Validate frontmatter, navigation, links, private/public boundaries, stable IDs and evidence references.
7. Update PentaDocs change, delta and reconciliation ledgers.
8. Update CHLOM, DAIL phase/release, master IP/asset, product or support records where required.
9. Commit repository source and record the exact branch, commit and pull request.
10. Merge and deploy through the governed documentation release path.
11. Read back the public provider route and expected content.
12. Append terminal release evidence and the next-review trigger.

A repository update without deployment is `MERGED` or `SOURCE_READY`, not `PUBLIC_CURRENT`. A deployment without exact readback is `DEPLOYED_PENDING_READBACK`.

## Internal-link continuity contract

A substantive current page should ordinarily carry at least two meaningful internal links. Preferred link classes are:

- source authority or evidence standard;
- canonical platform/framework/portfolio registry;
- applicable legal, rights, security or commerce standard;
- phase readiness or roadmap gate;
- alias/predecessor/successor/historical lineage;
- operational workflow or runbook.

Links are not decoration. They are continuity edges in the institutional knowledge graph.

A useful continuity edge answers at least one of these questions:

- **Where did this claim come from?**
- **Which record is canonical now?**
- **What governs this capability?**
- **What happens when it fails or changes?**
- **What does this block or enable next?**
- **What historical record explains the lineage?**

## 795-title Help Center rebuild

The recovered Help Center estate contains **795 titles** in the current forensic baseline. That number is a recovery universe, not the current PentaDocs page count.

Every recovered title must reach exactly one governed terminal disposition:

| Disposition | Meaning |
| --- | --- |
| `canonical_article` | substantive current article exists |
| `merged_successor` | content is incorporated into a stronger canonical article |
| `permanent_redirect` | old route points to the current successor |
| `restricted_record` | content belongs in authenticated/private knowledge rather than public docs |
| `superseded_history` | preserved as historical institutional evidence |
| `unresolved_source` | source is not sufficient to reconstruct safely |

A title, placeholder or navigation label does not count as a rebuilt article. See the [795-Article Disposition Seed](/support/help-center-795-article-disposition-seed) and [Articleization Contract](/support/articleization-manifest-and-autopublication-contract).

## Current Phase 3 execution contribution

Phase 3 — Execute is current. This framework contributes bounded documentation evidence without blanket-certifying any component; the earlier Phase 2.99 transition language is preserved as historical origin:

```text
source / current evidence
→ stale-state reconciliation
→ gap closure
→ continuity-link graph
→ article disposition
→ machine seed delta
→ validation
→ Phase 3 execution evidence
```

The historical [Phase 3 Readiness Gate](/technology/phase-3-readiness-gate) explains entry lineage. Current truth is governed by [Current Operational State](/start-here/current-operational-state), with subsystem holds remaining fail-closed until their exact evidence passes.

## Historical roadmap lineage and current PENTA horizon

Earlier ten- and twenty-phase roadmaps remain historical planning evidence. The current lifecycle is PENTA: Discover → Govern → Execute → Verify → Preserve.

This framework records impacts into Phase 4 — Verify and Phase 5 — Preserve. It does not reactivate retired decimal, ten-phase or twenty-phase namespaces. See the [PENTA Phase Model](/start-here/current-operational-state) and the historical [Twenty-Phase Horizon Reconciliation](/standards/twenty-phase-horizon-reconciliation).

## PentaFactory materialization

The framework package is designed to contain:

- framework manifest;
- governed skill;
- bounded tool contracts;
- article-rebuild contract;
- sprint receipt schema/data;
- validator;
- GitHub governance workflow;
- PentaDocs public-safe framework page (currently rendered through Mintlify);
- PentaFactory package record;
- compilation/test receipt;
- DAIL/future certification references where available.

Factory registration does not create production authority. Parent certification, security review, exact-head binding, CI and current Phase 3 subsystem gates remain separate.

## DAIL documentation profile

The Cursor-Controlled DAIL Capacity Mesh is a reference implementation of this framework. Its current documentation estate spans:

- ThriveBase runtime tables, scheduler state, manifests, Cursor runs, continuity state, MeshAssist, capacity-mesh and donor receipts;
- canonical DAIL events and terminal readbacks;
- the governed Drive DAIL Phase Library Master Ledger, complete compendium, evidence and publication workbook, phase books, diagrams and recovery records;
- versioned CrownThrive OS PentaDocs source;
- PentaDocs change, delta and reconciliation ledgers;
- a public-release state that remains separate until repository merge, provider deployment and exact route readback complete.

The governing architecture keeps one canonical DAIL chain and writer, Four-Part Cursor projections, the 500K Continuity Lock, PentaBalancer admission, PentaGas capacity accounting and bounded MeshAssist/donor capacity. Documentation of that architecture does not alter its runtime ceilings or authority boundaries.

See [CHLOM Overview](/chlom/overview) and [PentaDocs](/pentas/canonical/penta-docs).

## Related institutional controls

- [Documentation Source-of-Truth & Autonomous Governance](/standards/documentation-source-of-truth-and-autonomous-governance)
- [Platform Extraction & Next-Run Preparation](/standards/platform-extraction-and-next-run-prep)
- [Institutional Registry Architecture](/technology/institutional-registry-architecture)
- [Source Register](/knowledge/source-register)
- [Current-State Validation Queue](/knowledge/current-state-validation-queue)
- [Phase 2.0–2.9 Retroactive Reconciliation](/knowledge/phase-2-0-through-2-9-retroactive-reconciliation)
- [Ten-Phase Institutional Program Charter](/standards/ten-phase-institutional-program-charter)

## Strong-everywhere acceptance

Before stating that a documentation subject is strong everywhere, verify:

- Drive source exists in the intended governed folder.
- Repository source contains the current generation.
- Required navigation or a discoverable canonical entrypoint reaches the content.
- Internal links resolve.
- PentaDocs change, delta and reconciliation ledgers contain the release.
- CHLOM and DAIL phase/release records are current where applicable.
- The Master IP & Asset Registry contains newly created durable documentation assets.
- Repository review and merge state are recorded.
- Documentation-provider deployment and public route readback are recorded.
- Product guides, support pages, Go Flipbooks or downloadable packages are updated where the subject's release contract requires them.
- Historical versions remain preserved and explicitly superseded rather than silently overwritten.
- Documentation state is not used to manufacture runtime, provider or authority state.

## Success condition

A sprint succeeds when it leaves the scoped documentation **more current, more complete, more internally connected, more recoverable, more machine-readable and less dependent on conversational memory**, while preserving every unresolved state that the evidence cannot safely close.
