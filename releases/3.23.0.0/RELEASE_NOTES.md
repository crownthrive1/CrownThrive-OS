# CrownThrive OS 3.23.0.0 — Autonomous PentaRelease

Release generated by PentaRelease autonomous awareness and reconciled against the governed production lineage.

## Why this release exists

New executable capability or runtime delta.

PentaRelease selected **3.23.0.0** as a minor release after `v3.22.0.0`. The release cut is production commit `abe0ce883e86e7032d8b642bd405102f6a7ef4e1` and records the 25 main-side commits / 36 changed runtime and governance files observed from the `v3.22.0.0` comparison boundary.

## Lineage reconciliation

`v3.22.0.0` is not a direct ancestor of the current production branch because its one tag-side commit (`efafbb436852bf215183c2a3558d40b36d590e5f`) contains only the three PentaRelease materialization artifacts for 3.22.0.0. This release carries those exact three tagged artifacts into main as custody evidence. The existing `v3.22.0.0` tag is preserved and is not moved or rewritten.

Runtime lineage remains anchored at the shared merge base `3fb903404ceb1a92f0191bf5e810c70b04fce0d9`; no tag-only runtime code was omitted.

## Governance

- Versioning does not manufacture certification or authority.
- D3/breaking-authority changes remain HOLD for human governance.
- HOLD is never silently promoted to PASS.
- Secret-bearing paths are blocked from autonomous packaging.
- Provider readback is mandatory.
- Release-tag custody is preserved without retagging or rewriting historical evidence.
- The release inventory is cut at the production commit named above; later main commits belong to the next release decision unless explicitly reconciled before publication.
