# Copy this function-scoped entry into the project-level supabase/config.toml.
# Stripe uses exact-body HMAC authentication; it does not send a Supabase JWT.
[functions.dail-external-ingress]
verify_jwt = false
