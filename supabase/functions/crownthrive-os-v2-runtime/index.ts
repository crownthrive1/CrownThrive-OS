import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import postgres from "npm:postgres@3.4.3";
const PROJECT_URL=Deno.env.get("SUPABASE_URL")??"https://tzajnzshmtzjenqulehq.supabase.co";const SERVICE_ROLE=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"";let client:ReturnType<typeof postgres>|null=null;function db(){const url=Deno.env.get("SUPABASE_DB_URL")??"";if(!url)throw new Error("db_url_missing");client??=postgres(url,{max:2,idle_timeout:10,prepare:false});return client}function j(data:unknown,status=200){return new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store","x-content-type-options":"nosniff"}})}async function sha256(s:string){const d=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(s));return[...new Uint8Array(d)].map(b=>b.toString(16).padStart(2,"0")).join("")}async function authorized(req:Request){const token=req.headers.get("x-ct-os-token")??"";if(!token)return false;const h=await sha256(token);const rows=await db()<Array<{token_sha256:string;active:boolean;expires_at:string|null}>>`select token_sha256,active,expires_at from os_v2.runtime_tokens where token_name='ct_os_v2_runtime_token' limit 1`;const r=rows[0];return!!r&&r.active&&(!r.expires_at||Date.parse(r.expires_at)>Date.now())&&r.token_sha256===h}async function recordReceipt(taskId:string,kind:string,state:string,evidence:unknown,errorCode:string|null=null){const payload=JSON.stringify(evidence??{}),digest=await sha256(`${taskId}|${kind}|${state}|${payload}`);await db()`insert into os_v2.run_receipts(task_id,task_kind,result_state,evidence,evidence_sha256,error_code) values(${taskId}::uuid,${kind},${state},${db().json(evidence as any)},${digest},${errorCode})`}async function notify(subject:string,text:string,severity="info"){const sql=db();const preferences=await sql<Array<{recipient:string}>>`select recipient from os_v2.system_notification_preferences where preference_key='founder_primary' and enabled limit 1`;const recipient=preferences[0]?.recipient;if(!recipient)throw new Error('notification_recipient_unavailable');const rows=await sql<Array<{notification_id:string}>>`insert into os_v2.notifications(channel,recipient,subject,body,severity,state) values('email',${recipient},${subject},${text},${severity},'queued') returning notification_id::text`;return rows[0]?.notification_id}async function sendQueuedNotifications(limit=10){
  const sql=db();
  const boundedLimit=Math.min(2,Math.max(1,Number.isFinite(limit)?Math.trunc(limit):2));
  const rows=await sql<Array<{notification_id:string;recipient:string;subject:string;body:string;severity:string;trigger_ref:string}>>`select notification_id::text,recipient,subject,body,severity,trigger_ref from os_v2.claim_mail_notifications_v1(${boundedLimit})`;
  const out=[];
  for(const n of rows){
    try{
      const r=await fetch(`${PROJECT_URL}/functions/v1/mailgun-relay-control`,{
        method:'POST',
        headers:{authorization:`Bearer ${SERVICE_ROLE}`,'content-type':'application/json'},
        body:JSON.stringify({
          action:'send_internal',
          to:n.recipient,
          from_local:'notifications',
          subject:n.subject,
          text:n.body,
          trigger_ref:n.trigger_ref||'os-v2:notification',
          request_key:`os-v2-notification:${n.notification_id}`
        })
      });
      const t=await r.text();
      let p:any={};
      try{p=JSON.parse(t)}catch{p={raw:t.slice(0,500)}}
      const reason=String(p?.error??'mailgun_rejected');
      const governedDeferral=!r.ok&&p?.provider_call_made===false&&(
        reason.startsWith('provider_circuit_')||
        reason==='trigger_probation'||
        reason==='controlled_release_batch_limit'||
        reason==='rolling_hour_limit'
      );
      const probationDetected=!r.ok&&p?.provider_call_made===true&&p?.provider_probation_detected===true;
      if(r.ok){
        await sql`update os_v2.notifications set state='sent',sent_at=${new Date().toISOString()},last_error=null,provider_receipt=${sql.json(p)} where notification_id=${n.notification_id}::uuid`;
      }else if(probationDetected){
        const holdAt=Date.parse(String(p?.control?.hold_until??''));
        const triggerAt=Date.parse(String(p?.control?.trigger_probation_until??''));
        const retryAt=new Date(Math.max(Date.now()+300000,Number.isFinite(holdAt)?holdAt:0,Number.isFinite(triggerAt)?triggerAt:0));
        await sql`update os_v2.notifications set state='queued',available_at=${retryAt.toISOString()},last_error='governed_mailgun_probation_detected',provider_receipt=${sql.json(p)} where notification_id=${n.notification_id}::uuid`;
      }else if(governedDeferral){
        const retryRaw=String(p?.control?.retry_at??'');
        const retryParsed=Date.parse(retryRaw);
        const fallbackSeconds=Math.max(60,Math.min(Number(p?.retry_after_seconds??300),86400));
        const retryAt=Number.isFinite(retryParsed)?new Date(retryParsed):new Date(Date.now()+fallbackSeconds*1000);
        await sql`update os_v2.notifications set state='queued',attempt_count=greatest(attempt_count-1,0),available_at=${retryAt.toISOString()},last_error=${'governed_'+reason},provider_receipt=${sql.json(p)} where notification_id=${n.notification_id}::uuid`;
      }else{
        await sql`update os_v2.notifications set state='failed',last_error=${'mailgun_'+r.status},provider_receipt=${sql.json(p)} where notification_id=${n.notification_id}::uuid`;
      }
      out.push({id:n.notification_id,status:r.status,ok:r.ok,state:r.ok?'sent':probationDetected||governedDeferral?'queued':'failed',provider_call_made:p?.provider_call_made===true});
    }catch(e){
      await sql`update os_v2.notifications set state='failed',last_error=${e instanceof Error?e.message:'send_error'} where notification_id=${n.notification_id}::uuid`;
      out.push({id:n.notification_id,ok:false,state:'failed'});
    }
  }
  return out
}
async function runFactoryTick(){const secretRows=await db()<Array<{decrypted_secret:string}>>`select decrypted_secret from vault.decrypted_secrets where name='ct_factory_worker_token' order by created_at desc limit 1`;const workerToken=secretRows[0]?.decrypted_secret;if(!workerToken)return{state:'hold',evidence:{worker:'ct-software-factory-worker',code:'FACTORY_WORKER_TOKEN_UNAVAILABLE'}};const r=await fetch(`${PROJECT_URL}/functions/v1/ct-software-factory-worker`,{method:'POST',headers:{authorization:`Bearer ${SERVICE_ROLE}`,'content-type':'application/json','x-ct-factory-token':workerToken},body:JSON.stringify({limit:8}),signal:AbortSignal.timeout(110000)});const text=await r.text();let body:any={};try{body=JSON.parse(text)}catch{body={raw:text.slice(0,1000)}}if(!r.ok)return{state:'hold',evidence:{worker:'ct-software-factory-worker',http_status:r.status,response:body}};return{state:'pass',evidence:{worker:'ct-software-factory-worker',drained:Number(body?.drained??0),response:body}}}async function handleTask(task:any){const sql=db(),kind=String(task.task_kind);if(kind==='system_health'){const r=await sql`select now() as observed_at,(select count(*) from cron.job where active) as active_cron_jobs,(select count(*) from integration_control.services) as registered_services,(select count(*) from chlom_runtime.modules) as modules,(select count(*) from integration_control.thriveevergreen_mesh_work_queue_v1 where work_state in ('queued','running')) as open_mesh_work,(select count(*) from public.ct_factory_work_units where status in ('ready','leased','running')) as open_factory_work`;return{state:'pass',evidence:r[0]}}if(kind==='factory_tick')return await runFactoryTick();if(kind==='commerce_mesh'){const r=await sql`select integration_control.thriveevergreen_commerce_mesh_cycle_v1() as result`;return{state:'pass',evidence:r[0]?.result??{}}}if(kind==='scheduler_reconcile'){const r=await sql`select jobid,jobname,schedule,active from cron.job where jobname like 'ct-%' or jobname like 'crownthrive%' or jobname like 'chlom%' order by jobid`;const critical=['ct-crownthrive-os-v2-dispatch','ct-crownthrive-os-v2-watchdog','ct-software-factory-tick-v2'];const criticalState=critical.map(name=>({name,active:r.some((x:any)=>x.jobname===name&&x.active)})),missing=criticalState.filter(x=>!x.active);if(missing.length)await notify('CrownThrive OS V2 scheduler fault',`OS V2 detected inactive critical scheduler(s): ${missing.map(x=>x.name).join(', ')}. Runtime remained fail-closed and did not manufacture recovery authority.`,'critical');return{state:missing.length?'hold':'pass',evidence:{critical:criticalState,observed_jobs:r.length}}}if(kind==='self_repair'){const stale=await sql`update os_v2.tasks set state='queued',claimed_at=null,claimed_by=null,available_at=now(),last_error='recovered_stale_lease' where state='running' and claimed_at<now()-interval '15 minutes' and risk_class in ('D0','D1','D2') returning task_id::text`;const fs=await sql`update public.ct_factory_work_units set status='ready',lease_until=null where status='leased' and lease_until<now() returning id::text`;return{state:'pass',evidence:{stale_os_tasks_requeued:stale.length,stale_factory_leases_requeued:fs.length}}}if(kind==='notification_flush'){const sent=await sendQueuedNotifications(10);return{state:'pass',evidence:{processed:sent.length,results:sent}}}if(kind==='knowledge_projection'){const r=await sql`select (select count(*) from chlom_runtime.dail_events) as dail_events,(select count(*) from chlom_runtime.project_portfolio_projects) as projects,(select count(*) from integration_control.site_mesh_bindings) as site_bindings,(select count(*) from public.ct_factory_artifacts) as factory_artifacts,(select count(*) from public.ct_factory_release_packages) as factory_release_packages`;return{state:'pass',evidence:r[0]}}return{state:'hold',evidence:{reason:'unsupported_task_kind',task_kind:kind}}}async function processTasks(limit=12){const sql=db();const claimed=await sql<Array<any>>`with c as (select task_id from os_v2.tasks where state='queued' and available_at<=now() order by priority desc,created_at limit ${limit} for update skip locked) update os_v2.tasks t set state='running',claimed_at=now(),claimed_by='ct.os.v2.runtime',attempt_count=attempt_count+1 from c where t.task_id=c.task_id returning t.*`;const results=[];for(const task of claimed){try{if(task.risk_class==='D3'){await sql`update os_v2.tasks set state='hold',completed_at=now(),last_error='D3_HUMAN_RESERVED' where task_id=${task.task_id}`;await recordReceipt(task.task_id,task.task_kind,'hold',{reason:'D3_HUMAN_RESERVED'},'D3_HUMAN_RESERVED');results.push({task_id:task.task_id,state:'hold'});continue}const rr=await handleTask(task);await sql`update os_v2.tasks set state=${rr.state==='pass'?'completed':rr.state},completed_at=now(),result=${sql.json(rr.evidence)},last_error=${rr.state==='pass'?null:'TASK_HOLD'} where task_id=${task.task_id}`;await recordReceipt(task.task_id,task.task_kind,rr.state,rr.evidence,rr.state==='pass'?null:'TASK_HOLD');results.push({task_id:task.task_id,kind:task.task_kind,state:rr.state})}catch(e){const msg=e instanceof Error?e.message:'task_error',terminal=Number(task.attempt_count)>=Number(task.max_attempts);await sql`update os_v2.tasks set state=${terminal?'failed':'queued'},available_at=now()+interval '5 minutes',completed_at=${terminal?new Date().toISOString():null},last_error=${msg} where task_id=${task.task_id}`;await recordReceipt(task.task_id,task.task_kind,'failed',{error:msg,terminal},msg);if(terminal)await notify('CrownThrive OS V2 task failure',`Task ${task.task_kind} (${task.task_id}) exhausted retries. Error: ${msg}`,'critical');results.push({task_id:task.task_id,state:terminal?'failed':'retry',error:msg})}}return results}Deno.serve(async(req:Request)=>{try{if(req.method==='GET'){const rows=await db()`select version,state,last_heartbeat_at,release_state from os_v2.runtime_state where singleton=true`;return j({service:'crownthrive-os-v2-runtime',...rows[0],factory_worker:'wired'})}if(req.method!=='POST')return j({error:'method_not_allowed'},405);if(!(await authorized(req)))return j({error:'runtime_auth_required'},403);const body=await req.json().catch(()=>({})) as any,action=String(body.action??'tick');if(action==='tick'){const processed=await processTasks(Math.min(25,Math.max(1,Number(body.limit??12))));await db()`update os_v2.runtime_state set state='hot',last_heartbeat_at=now(),last_tick_at=now(),updated_at=now() where singleton=true`;return j({service:'crownthrive-os-v2-runtime',version:'2.1.2',state:'hot',factory_worker:'wired',processed})}if(action==='flush_notifications')return j({state:'complete',results:await sendQueuedNotifications(20)});return j({error:'unsupported_action'},400)}catch(e){return j({error:'runtime_error',detail:e instanceof Error?e.message:'unknown'},500)}});
