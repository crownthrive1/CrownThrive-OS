name: Penta PR Gate Awareness

on:
  pull_request_target:
    types: [opened, reopened, synchronize, ready_for_review]
  workflow_dispatch:
    inputs:
      number:
        description: Pull request number to reconcile
        required: true
        type: string

# Observation/projection only. PentaPM owns Development metadata. This workflow
# must not create Issues, edit PR bodies, push evidence commits, or certify the
# candidate. Independent security, governance, provider, and certifier gates
# remain mandatory.
permissions:
  contents: read
  pull-requests: read
  issues: read
  checks: read
  statuses: read
  actions: read

concurrency:
  group: penta-pr-gate-awareness-${{ github.repository }}-${{ github.event.pull_request.number || inputs.number }}
  cancel-in-progress: true

jobs:
  gate-awareness:
    name: Penta originator readiness projection
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Resolve exact same-repository PR target
        id: target
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          EVENT_NUMBER: ${{ github.event.pull_request.number }}
          INPUT_NUMBER: ${{ inputs.number }}
        run: |
          set -euo pipefail
          python - <<'PY'
          import json, os, urllib.request

          repo = os.environ["REPOSITORY"]
          number = (os.environ.get("EVENT_NUMBER") or os.environ.get("INPUT_NUMBER") or "").strip()
          if not number.isdigit():
              raise SystemExit("pull_request_number_required")
          request = urllib.request.Request(
              f"https://api.github.com/repos/{repo}/pulls/{number}",
              headers={
                  "Accept": "application/vnd.github+json",
                  "Authorization": f"Bearer {os.environ['GITHUB_TOKEN']}",
                  "X-GitHub-Api-Version": "2022-11-28",
                  "User-Agent": "CrownThrive-Penta-Gate-Awareness/2.1",
              },
          )
          with urllib.request.urlopen(request, timeout=30) as response:
              pull = json.load(response)
          if pull.get("state") != "open":
              raise SystemExit("pull_request_not_open")
          if (((pull.get("head") or {}).get("repo") or {}).get("full_name")) != repo:
              raise SystemExit("external_fork_projection_fail_closed")
          head = (pull.get("head") or {}).get("sha") or ""
          base = (pull.get("base") or {}).get("sha") or ""
          head_ref = (pull.get("head") or {}).get("ref") or ""
          originator = ((pull.get("user") or {}).get("login") or "unknown-originator").strip()
          if len(head) != 40 or len(base) != 40 or not head_ref:
              raise SystemExit("exact_pr_identity_unresolved")
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as fh:
              fh.write(f"number={int(number)}\n")
              fh.write(f"head_sha={head}\n")
              fh.write(f"base_sha={base}\n")
              fh.write(f"head_ref={head_ref}\n")
              fh.write(f"originator={originator}\n")
          PY

      - name: Check out canonical Penta control plane
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: crownthrive1/CrownThrive-OS
          ref: main
          persist-credentials: false
          path: penta-control

      - name: Check out exact candidate head read-only
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ github.repository }}
          ref: ${{ steps.target.outputs.head_sha }}
          persist-credentials: false
          fetch-depth: 0
          path: pr-worktree

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: '3.12'

      - name: Generate exact-head non-certifying originator-readiness packet
        shell: bash
        env:
          REPOSITORY: ${{ github.repository }}
          NUMBER: ${{ steps.target.outputs.number }}
          BASE_SHA: ${{ steps.target.outputs.base_sha }}
          HEAD_SHA: ${{ steps.target.outputs.head_sha }}
          ORIGINATOR: ${{ steps.target.outputs.originator }}
        run: |
          set -euo pipefail
          cd pr-worktree
          observed="$(git rev-parse HEAD)"
          if [[ "$observed" != "$HEAD_SHA" ]]; then
            echo "::error::PR head moved during readiness projection"
            exit 75
          fi
          git fetch --no-tags --no-recurse-submodules origin "$BASE_SHA" "$HEAD_SHA"
          python ../penta-control/scripts/penta_pr_gate_awareness.py prepare \
            --repo . \
            --repository "$REPOSITORY" \
            --base "$BASE_SHA" \
            --head "$HEAD_SHA" \
            --pr-number "$NUMBER" \
            --originator "$ORIGINATOR" \
            > "$RUNNER_TEMP/penta-pr-originator-readiness.json"
          python - <<'PY'
          import json, os
          with open(os.path.join(os.environ["RUNNER_TEMP"], "penta-pr-originator-readiness.json"), encoding="utf-8") as fh:
              result = json.load(fh)
          if result.get("authority_created") is not False:
              raise SystemExit("originator_readiness_authority_boundary_failed")
          if result.get("independent_certification_required") is not True:
              raise SystemExit("independent_certification_requirement_missing")
          PY
          cat "$RUNNER_TEMP/penta-pr-originator-readiness.json"

      - name: Upload immutable workflow evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: penta-pr-originator-readiness-${{ steps.target.outputs.number }}-${{ steps.target.outputs.head_sha }}
          path: |
            ${{ runner.temp }}/penta-pr-originator-readiness.json
            pr-worktree/penta/evidence/pr-originator-readiness/pr-${{ steps.target.outputs.number }}.json
            pr-worktree/penta/continuity/receipts/pr-${{ steps.target.outputs.number }}-originator-readiness.json
          if-no-files-found: error
          retention-days: 90

      - name: Project ownership boundary
        if: always()
        shell: bash
        run: |
          {
            echo '## Penta PR Gate Awareness v2.1'
            echo
            echo "- PR: #${{ steps.target.outputs.number }}"
            echo "- Exact head: \`${{ steps.target.outputs.head_sha }}\`"
            echo '- Development metadata owner: **PentaPM only**'
            echo '- Candidate branch mutation: **disabled**'
            echo '- Issue creation: **disabled**'
            echo '- PR body mutation: **disabled**'
            echo '- Originator readiness: workflow evidence only; authority_created=false'
            echo '- Independent certification: **still required**'
          } >> "$GITHUB_STEP_SUMMARY"
