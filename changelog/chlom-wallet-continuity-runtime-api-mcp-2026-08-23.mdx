---
title: CHLOM Wallet Continuity Runtime API & MCP — 2026-08-23
description: Controlled-test private runtime binding for the continuity HTTP and MCP surfaces.
---

# CHLOM Wallet Continuity Runtime API & MCP institutionalized

This tranche converts the source-complete Phase E API/MCP contracts into a deployable, service-role-only runtime while preserving every continuity authority boundary.

Implemented:

- Supabase Edge Function `chlom-wallet-continuity`;
- eight private HTTP routes;
- MCP protocol revision `2026-07-28` with `server/discover`, `tools/list`, and `tools/call`;
- exact `Mcp-Method` / `Mcp-Name` header-to-body validation;
- eight governed continuity tools mapped to service-role-only Data API RPCs;
- append-only API/MCP request digest receipts;
- append-only runtime release and canary ledgers;
- oracle-observation and recovery-plan RPCs that remain read-only/non-destructive outside private evidence state;
- 20,000 adversarial header-routing cases with zero accepted mismatches;
- source-controlled CI, documentation, runtime SQL, and machine manifest.

The deployment may be recorded as deployed before an authenticated runtime canary exists, but it may not be promoted to `BOUND_CONTROLLED_TEST` until that canary is independently recorded against the exact source head and Edge Function version.

No provider write, money movement, Rights grant, chain broadcast, credential disclosure, checkout, destructive recovery, production activation, merge authority, or phase advancement is authorized.
