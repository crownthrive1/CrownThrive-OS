---
title: "CHLOM Wallet Governance and ThriveBase Naming Hardening"
sidebarTitle: "Wallet Governance Hardening"
description: "Fail-closed correction of governed Git-diff evidence availability, canonical ThriveBase naming, and policy-assurance runtime readback."
icon: "shield-check"
---

# CHLOM Wallet governance and ThriveBase naming hardening

**Date:** 2026-08-23  
**Institutional phase:** 2.99  
**PR:** #230  
**State:** `DRAFT_HOLD`  
**Founder action required now:** false

This tranche hardens two controls before additional WalletKit expansion:

1. the governed merge gate now preserves the complete Git history needed to resolve exact pull-request base and head objects;
2. a newly added policy-assurance readback artifact used a retired missing-r spelling of ThriveBase.

## Governed diff hardening

The merge gate checks out complete history with credentials disabled, proves that provider-reported base and head objects exist locally, and binds the decision engine to the exact base-to-head file set.

This does not broaden repository authority. It removes false CI failures caused by missing local Git objects while preserving the trusted-diff requirement.

## Canonical ThriveBase naming contract

The canonical forms are now machine-enforced:

```yaml
human_label: ThriveBase
machine_token: thrivebase
retired_missing_r_variant: prohibited_in_current_source
```

The validator scans all tracked text and paths. It runs in Documentation Governance and the Governed Merge Gate. New source may not bind the retired variant as an executable identity, filename, schema, function, slug, or current documentation label.

## Runtime compatibility boundary

Read-only inspection found a pre-existing compatibility estate using the retired variant:

```yaml
active_edge_aliases: 1
public_routine_aliases: 8
schema_aliases: 2
trigger_aliases: 1
pgmq_relation_aliases: 7
state: QUARANTINED_PENDING_DEPENDENCY_TESTED_CUTOVER
```

Those runtime objects were not destructively renamed in this tranche. A safe cutover requires dependency inventory, canonical wrappers, behavior-equivalence canaries, trigger and consumer rebinding, rollback readback, and explicit retirement approval. New dependencies may not bind the legacy aliases.

## Policy-assurance runtime readback

The private runtime was read back without mutation:

```yaml
tables: 4
algorithms: 7
rulepacks: 1
decision_receipts: 3
latest_canary: PASS_CHLOM_WALLET_POLICY_ASSURANCE_RUNTIME_CANARY
```

The source-controlled readback path and result identity now use the canonical ThriveBase form.

## Preserved boundaries

```yaml
production_activation: false
provider_write: false
credential_access_created: false
custody: false
token_issuance: false
money_movement: false
production_rights_grant: false
chain_broadcast: false
effective_price_publication: false
checkout_activation: false
phase_advancement: false
merge_authorized: false
```

Independent reviewers remain separate. Their heartbeats and review receipts were not manufactured or refreshed by this tranche.
