---
title: "Collision governance RTC v2 controlled-test packet"
description: "Current-main successor with prevention, version-bound awareness, fenced repair planning, and bounded reaction."
standard_version: "1.0.0"
primary_audience: "historical"
page_type: "changelog"
content_state: "historical"
---

{/* pentadocs:audience-orientation:v2 */}
<Warning>
  **Audience:** governance reviewers and researchers (`historical`).

  **This page:** Collision governance RTC v2 controlled-test packet — Current-main successor with prevention, version-bound awareness, fenced repair planning, and bounded reaction.

  **Documentation state:** `historical` for page type `changelog`. Documentation state is editorial posture only; it does not certify runtime, deployment, legal, rights, financial, provider, production, release, security, or operational status. This page is dated context, not current operating authority.

  **Related guidance:** [Current operational state](/start-here/current-operational-state) and [Source authority hierarchy](/knowledge/source-authority-hierarchy).
</Warning>
{/* /pentadocs:audience-orientation:v2 */}

## Status

`DRAFT / HOLD / controlled test`. No merge or acceptance is claimed.

## Supersession

- Predecessor: PR #158 at `7fe2a420ec62221bc977759f180880a138908475`.
- Predecessor disposition: stale, detection-only proposal; do not merge unchanged.
- V2 parent: `ct.agent.architecture-refactor-optimizer`.
- Founder Orchestrator: retained as a routing/escalation candidate, not activated as a competing runtime parent.

## Added

- deterministic structured intent and collision classifier;
- symmetric collision fingerprints;
- explicit CT-COLL-0 through CT-COLL-5 dispositions;
- start/end main and candidate-head readback;
- fail-closed incomplete semantic inspection;
- SHA-bound RTC event envelope with idempotency, correlation, causation, sequence, and hash-chain fields;
- single-owner lease contract with TTL and monotonic fencing;
- bounded repair state machine, rollback, retry, and dead letter;
- trusted-base live scanning physically isolated from no-token candidate tests in separate workflow files;
- no second scheduler slot; hourly recovery remains a subroute of the existing private CHLOM control cycle;
- PR lifecycle, main-push, merge-queue, and hourly recovery triggers;
- adversarial, concurrency, stale-writer, authority-boundary, and replay tests.

## Corrected from v1

- Semantic and runtime overlaps can no longer be downgraded merely because work is described as stacked.
- CT-COLL-3 and CT-COLL-4 now HOLD by default.
- Content-aware parsing covers declared stable IDs, routes, schedules, SQL resources, and explicit PR-body claims.
- Main/head movement invalidates the observation instead of allowing a mixed snapshot.
- Candidate code never receives `GITHUB_TOKEN` or persisted Git credentials.
- Runtime and repair claims now describe executable controls rather than advisory log output.

## Evidence boundary

The supplied v1 recovery package and detached v2 validation/checksum artifacts remain separate generations. The detached v2 PASS is not used to certify v1, this code, runtime activation, governance acceptance, or public deployment.
