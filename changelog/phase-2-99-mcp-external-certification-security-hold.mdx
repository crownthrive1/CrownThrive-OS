---
title: "MCP external certification security hold"
description: "Public-safe record of the CT-MCP-EXTCERT-001 remediation boundary."
---

## Current state

CT-MCP-EXTCERT-001 is on `SECURITY_HOLD`. The earlier payload and attestation are historical evidence only and cannot authorize capture, deployment, provider access, conformance execution, schema migration, certification PASS, or Phase 3 advancement after material security remediation.

## Public/private projection

The public repository retains only the certification identifier, protocol/version target, aggregate resource limits, acceptance-predicate identifiers, safe interfaces, lifecycle state, and evidence digests. Production identities, authorization architecture, provider-operation topology, runtime locators, derived schemas/migrations, credentials, private evidence, and protected implementation belong in governed private custody.

## Controls added

- workflow dispatch and OIDC minting are absent from the public hold workflow;
- provider reads and writes are disabled;
- the conformance proxy enforces request and response byte ceilings;
- upstream requests have an explicit timeout and downstream cancellation propagation;
- capture, schema inference, migration generation, and live execution fail closed;
- public CI records hashes and negative-test outcomes, never derived evidence bodies.

## Reopen criteria

A replacement packet requires private-custody restoration, exact-artifact IP/rights classification, Security/Privacy and Operations/SRE review, negative resource/cancellation tests, a new exact payload digest, a fresh named founder attestation for that digest, and current-head A/B/C/D/S governance including Agent D with no block. Phase 3 remains separately blocked by the Phase 2.99 hard exit and full-documentation gate.


## Current-main reconciliation

This draft now reconciles physical main `0ccc6fac3d7b7028f4bd48b095c6cb4c30fe263d` and its PR #200 correction artifacts without reusing either historical payload as authority. The two public correction records are removed from the candidate tree, the public runtime remains a non-deployable 503 security hold, and negative tests require those records to stay absent. The aggregate capture record is retained only as historical evidence: 12 CrownThrive IO reads, 8 ThriveTools SEO reads, zero writes, final execution skipped, and replay unauthorized.

This reconciliation is build evidence only. It does not create an Agent C vote, specialist acceptance, issue #131 disposition, founder ratification, quorum, merge, provider execution, certification, commerce authority, or Phase 3 entry.
