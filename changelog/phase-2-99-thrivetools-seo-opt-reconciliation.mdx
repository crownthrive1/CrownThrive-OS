---
title: "Phase 2.99 — ThriveTools SEO/OPT Child-Capability Reconciliation"
sidebarTitle: "2.99 ThriveTools SEO/OPT"
description: "Founder-authorized Phase 2.99 reconciliation resolving ThriveTools SEO and ThriveTools OPT as child capabilities of the ThriveTools parent and crosswalking their subdomains and OPT API evidence without altering historical source counts."
icon: "sitemap"
keywords: ["Phase 2.99", "ThriveTools", "ThriveTools SEO", "ThriveTools OPT", "subdomains", "domain manifest", "API", "reconciliation"]
---

# Phase 2.99 — ThriveTools SEO/OPT Child-Capability Reconciliation

## Decision

Founder authorization on August 21, 2026 resolves the outstanding ThriveTools parent/child classification:

```yaml
parent:
  id: ct.platform.thrivetools
  name: ThriveTools
children:
  - id: ct.capability.thrivetools.seo
    name: ThriveTools SEO
    relationship: capability_of
    host: https://seo.thrivetools.io
  - id: ct.capability.thrivetools.opt
    name: ThriveTools OPT
    relationship: capability_of
    host: https://optimize.thrivetools.io
counting_rule: no_new_top_level_platforms
```

The decision preserves the recovered source labels while establishing the current institutional graph.

## Evidence crosswalk

- `S107` independently preserves the three source labels: ThriveTools, ThriveTools SEO and ThriveTools OPT.
- `S108` records `thrivetools.io` as a primary domain and `seo.thrivetools.io` plus `optimize.thrivetools.io` as subdomains.
- `S109` records current public-safe ThriveTools OPT v4.0.0/API-control evidence.

The S100 historical count rule is preserved: `S100-DOM-057` remains the root `thrivetools.io` row in the 82-domain census. The two later subdomains become descendant host edges; they do not retroactively create synthetic S100 rows or top-level platform counts.

## OPT current boundary

`S109` supports REST base `https://optimize.thrivetools.io/api`, API documentation, server-side credential binding and an authenticated `GET /check-status` read. Remaining bounded D0 reads, request-body bounding, recovery/continuity and MCP exposure remain open. Upload/delete provider writes remain closed.

## SEO current boundary

The SEO child identity and host lineage are resolved. Current SEO application/provider/version/repository/deployment/API/data/write state remains verification-open because a domain/subdomain manifest cannot certify runtime capability.

## Downstream propagation

This adjudication updates or constrains:

- Holdings 68-row identity seed and reconciliation checkpoint;
- S103/S100 engine-domain edge register;
- Holdings 82-domain source lineage;
- Platform State register;
- Platform API/Adapter Matrix;
- API Base URL and Endpoint Seed Register;
- Current-State Validation Queue;
- Legal Coverage Deployment Register;
- Phase 2.99 plan;
- Phase 3 readiness gate;
- Ten-Phase Institutional Program Charter;
- ThriveTools Institutional Registry.

## Gate

```yaml
identity_ambiguity: closed
subdomain_lineage: closed_source_backed
seo_runtime_verification: open
opt_runtime_verification: partial_open
phase_2_99_hard_exit: unchanged
phase_3_entry: blocked_pending_phase_2_99_hard_exit
```

This is D1 identity/source reconciliation. It authorizes no secret exposure, provider write, pricing change, commerce activation, legal grant or Phase 3 bypass.