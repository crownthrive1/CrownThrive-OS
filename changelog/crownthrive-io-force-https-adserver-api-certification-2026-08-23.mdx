---
title: "crownthrive.io Force HTTPS + Adserver API Certification"
sidebarTitle: "HTTPS + Adserver API"
description: "Public-safe institutionalization record for all-account Force HTTPS state and Adserver.Online authenticated bounded API reads."
icon: "shield-check"
tag: "Certified read"
keywords: ["crownthrive.io", "cPanel", "Force HTTPS", "Adserver.Online", "AdLuxe", "API certification", "S116", "S117"]
---

# crownthrive.io Force HTTPS \+ Adserver API Certification

**Effective date:** August 22–23, 2026<br />**Founder authority:** Kavonte Jones Sr.<br />**Source refs:** `S116`, `S117`

## crownthrive.io cPanel control

Founder-attested current provider control state and THIVEBASE readback establish:

```yaml
identity_inventory_count: 12
force_https_on: 12
force_https_off: 0
force_https_unknown: 0
identity_inventory_changed: false
```

The exact account identities are `api.crownthrive.io`, `apps.crownthrive.io`, `beautybrdg.com`, `crownlinks.bio`, `crownlinks.io`, `crownlinks.xyz`, `crownthrive.io`, `gentsgrowth.com`, `mywellnesspath.me`, `naturalhair.me`, `sprint.crownthrive.io`, and `thrivelnk.io`.

This closes the cPanel Force HTTPS control-setting unknowns. It does not independently certify registrar custody, DNS authority, certificate validity, mail, deployment, or application health.

## Adserver.Online provider API

Official provider OpenAPI and bounded live tests now establish:

```yaml
provider_api_version: 2.4.1
production_base: https://api.adsrv.net/v2
authentication: Bearer_server_side
provider_rate_limit: 100_requests_per_minute_per_token
credential_custody: verified_dual_reference_vault
adapter: adluxe-api-control_v9
platform_jwt_required: true
provider_writes_enabled: false
```

Verified read canaries:

| Method | Path | Result | Public-safe evidence |
| --- | --- | --- | --- |
| `GET` | `/dict` | HTTP 200 JSON | status, latency, provider rate-limit headers, 13-group structural shape, response size and digest |
| `GET` | `/stats` | HTTP 200 JSON | one-day UTC bounded report; status, latency, row count, field names, response size and digest |

No provider credential or raw provider response body entered public documentation. The permanent adapter applies platform JWT enforcement, internal administrator/service authorization, streaming request and provider-response ceilings, a provider timeout, an explicit read allowlist and audit receipts.

## Governing boundary

### PASS

- cPanel Force HTTPS setting for all 12 account identities;
- Adserver.Online API contract/base/auth/rate-limit extraction;
- Vault dual-reference credential continuity;
- authenticated bounded `GET /dict`;
- authenticated bounded `GET /stats`;
- AdLuxe reporting adapter v9;
- DAIL evidence and drift-watched reconciliation tags.

### OPEN / NOT-PASS

- Adserver public catalog versus CrownThrive restricted subscription/billing-contract reconciliation;
- wider provider read families, campaign delivery, inventory, settlements and retention-window certification;
- registrar, DNS, certificate and application-health certification for the cPanel identities.

### CLOSED / FAIL-CLOSED

- Adserver/AdLuxe provider writes;
- campaign, site, zone, user, payment, payout, transaction and other mutation families;
- raw provider-body or secret publication.

Provider API authentication does not manufacture advertising, billing, settlement, campaign, rights, or commerce authority.