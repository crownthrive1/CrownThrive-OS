---
title: "Plugin–Pallet–Kernel Asset Fabric controlled-test checkpoint"
description: "Public-safe checkpoint for 5,760 derived package candidates, the ThriveAssetGovernor mesh, cross-platform tooling and independent scrutiny."
---

# Plugin–Pallet–Kernel Asset Fabric checkpoint

CrownThrive now has a deterministic package layer for plugins, pallet bundles, kernels, executable profiles, scripts, skills, prompts, MCP packs, API adapters, event handlers, schema contracts, and test suites.

## Delivered

- 12 pallet plugin lanes;
- 120 capability families;
- 12 asset types;
- four deployment profiles;
- 5,760 machine-readable candidate records;
- 8,640 dependency edges;
- deterministic catalog generation and per-pallet digests;
- a root Asset Fabric control plugin with 20 tools;
- seven non-voting A2/D2 control agents;
- cross-platform CLI launchers;
- generator, validator, independent scrutinizer, runtime validator, and direct-file materializer;
- database schema/RPC contracts;
- repository governance workflow;
- public-safe documentation and skill contract.

## Validation

A complete local materialization passed exact count, stable-ID, shard/root digest, dependency, owner/verifier, secret/private-field, authority, and no-commerce/no-D3 controls.

The independent result is `PASS_WITH_CONTROLLED_TEST_HOLDS`.

## Counting boundary

The 5,760 records are derived package candidates. They do not silently add 5,760 independent source-IP claims to the authoritative 100,800-asset Proprietary Asset Factory count.

## Holds

Every candidate still requires applicable rights, security, tests, custody, runtime, accessibility, fulfillment, and independent-verification evidence before promotion.

Checkout, entitlements, public plugin submission, provider writes, D3 automation, sovereign-vote effects, and direct-main merge remain disabled.

The repository stores the compact source matrix and cryptographic receipt. Expanded records remain deterministically reproducible on demand and are materialized into THIVEBASE rather than committed as static repository bloat.
