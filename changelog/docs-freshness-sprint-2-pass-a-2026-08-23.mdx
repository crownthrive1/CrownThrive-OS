---
title: "Docs Freshness Sprint 2 — Pass A Stale-State Reconciliation"
sidebarTitle: "Docs Sprint 2 — Pass A"
description: "Sprint 2 first-pass stale-state reconciliation across unlisted pages, provider/version drift, lifecycle/runtime state, domain/API evidence, P0/P1 platform intelligence and Phase 3 machine-seed readiness."
icon: "clock-rotate-left"
keywords: ["Sprint 2", "stale docs", "provider drift", "Phase 3", "Mintlify", "ThriveBase", "unlisted pages", "API state"]
standard_version: "1.0.0"
primary_audience: "historical"
page_type: "changelog"
content_state: "historical"
---

{/* pentadocs:audience-orientation:v2 */}
<Warning>
  **Audience:** governance reviewers and researchers (`historical`).

  **This page:** Docs Freshness Sprint 2 — Pass A Stale-State Reconciliation — Sprint 2 first-pass stale-state reconciliation across unlisted pages, provider/version drift, lifecycle/runtime state, domain/API evidence, P0/P1 platform intelligence and Phase 3 machine-seed readiness.

  **Documentation state:** `historical` for page type `changelog`. Documentation state is editorial posture only; it does not certify runtime, deployment, legal, rights, financial, provider, production, release, security, or operational status. This page is dated context, not current operating authority.

  **Related guidance:** [Current operational state](/start-here/current-operational-state) and [Source authority hierarchy](/knowledge/source-authority-hierarchy).
</Warning>
{/* /pentadocs:audience-orientation:v2 */}

## Docs Freshness Sprint 2 — Pass A Stale-State Reconciliation

Sprint 2 applies the permanent two-pass [Documentation Reconciliation & Continuity Framework](/standards/documentation-reconciliation-continuity-framework) to a 30-page high-value cohort.

## Scope

Pass A focuses on stale current state across:

- unlisted substantive documentation;
- provider and installed-version drift;
- lifecycle/runtime state;
- domain and API state;
- current-versus-target capability language;
- P0/P1 platform intelligence;
- public claims;
- Phase 3 machine-seed readiness.

## Major corrections

### Unlisted documentation

All nine substantive pages reported as unlisted by the prior Documentation Governance run are now represented in navigation. These include current automation architecture, sales-email skills, Locticians claim conversion, domain/deployment certification, MCP security-hold evidence and their related changelog records.

### Mintlify current state

Fresh Admin MCP readback supersedes older custom-domain and source-check assumptions:

```yaml
source_repo: crownthrive1/CrownThrive-OS
source_branch: main
custom_domains: []
deployment_doc_version: 89
mintlify_link_rot: disabled
mintlify_vale: disabled
weekly_link_integrity: disabled
typo_workflow: disabled
seo_workflow: disabled
governed_non_automerge_workflows_active: 3
```

The default Mintlify deployment remains the working documentation surface. `docs.crownthrive.io` stays a future target and is not treated as a Phase 3 prerequisite.

### ThriveInstitutionalize lifecycle

Fresh ThriveBase readback replaces the stale `HOLD / NOT-LIVE` conclusion:

- ten lifecycle stage definitions exist;
- one activation baseline exists;
- current lifecycle receipts remain zero;
- runtime is activated in controlled test;
- current generation is 48;
- next generation is 49;
- governed candidate ceiling is generation 60;
- production activation and checkout remain disabled;
- D3 remains human-reserved.

Zero receipts means generation 48 has not inherited lifecycle PASS from earlier generations.

### Shared institutional runtime

Virality Music, KJV Visualized/Sermon Toolkit, CrownPulse and ThriveTools now have documented `controlled_test` bindings to the shared CHLOM institutional capability runtime. Production activation remains false for each.

Virality and KJV also have controlled-test identity and DAIL bindings. Their DLA lanes remain specified/pilot-pending; Virality LEX remains specified/pilot-pending.

### Locticians provider/API state

The underlying directory/community provider is now documented as Brilliant Directories. Existing API credential authentication and exact Site Info permission failure are separated: authentication passes, Site Info remains HTTP 403 until the required Advanced API permission is enabled and independently read back. Founder authorization covers only that needed permission; provider execution/readback remains pending and writes stay closed.

### AdLuxe provider/API state

The developer matrix now matches the current Adserver.Online evidence: OpenAPI 2.4.1, bounded authenticated `GET /dict` and one-day `GET /stats` reads through `adluxe-api-control` v9, with provider writes closed and campaign/delivery/settlement truth independently gated.

### Hosting control state

The crownthrive.io domain/deployment packet no longer describes cPanel API read as failing. Current evidence includes bounded authenticated cPanel UAPI and WHM reads plus safe explicit-FTPS read evidence. DNS/provider mutation and rollback certification remain separate HOLDs.

## Phase 3 effect

This pass materially improves the Phase 3 input substrate by replacing generic unknown/stale states with exact controlled-test, PASS, HOLD and pending boundaries. It does not make Phase 3 eligible by itself.

```yaml
phase_3_entry: blocked_pending_phase_2_99_hard_exit
phase_11_20: reserved_definition_required
```

## Next automatic pass

Sprint 2 Pass B now closes evidence-backed documentation gaps, adds internal-link continuity across the exact scope, maps the cohort into the 795-title article reconstruction estate, validates the result and emits the final sprint receipt.
