# PentaGuardian

PentaGuardian is CrownThrive OS's active non-destructive technical guardian for governed child repositories, framework children, repository references, capability dependencies, and future registered child resources.

## Stable machine identity

- Penta: `ct.agent.repository-child-guardian-ad-litem`
- Class: `ct.agent-class.repository-guardian-ad-litem.v1`
- Family: `ct.family.repository-child-guardian.v1`
- Framework package: `ct.framework-package.repository-child-guardian-ad-litem`
- Production activation receipt: `5885c86a-2bbc-42d0-b5e1-a5b104213010`
- Runtime family cycle: `chlom_runtime.repository_child_guardian_family_cycle_v1()`
- Scheduler: `ct-repository-child-guardian-30m`

`PentaGuardian` is the canonical human-facing name. The stable machine IDs are preserved for compatibility and evidence continuity.

## Current operational truth

The existing production package is `maintained` and operationally enabled. PentaGuardian's agent projection is `active`, its guardian class is `active`, and its family is `certified` with `operational_state=active`.

The active cycle reports `PASS_ACTIVE` only when the production package, agent projection, guardian class, family state, child-guardian cycle, family rebuild, and CIE post-activation assurance all satisfy their bounded predicates.

## Authority boundary

PentaGuardian does not receive authority from its family titles or presentation metaphors. It remains D2-bounded, non-voting, no-self-approval, and independently verifiable.

Forbidden authority remains unchanged:

- merge
- delete
- archive
- transfer
- visibility change
- child self-activation
- self-approval
- vote
- D3 actions
- manufactured provider, commerce, rights, or money authority

The family relationship vocabulary is presentation and topology metadata only; it does not create legal, biological, sovereign, governance, or execution authority.

## Evidence

The active-state projection is a reconciliation to existing production truth, not a new activation. Canonical DAIL reconciliation event:

- event: `0b2a17d9-67d8-408f-80ca-cfce432f8729`
- hash: `359a7a6e91c6718b10912efc060617c3bb97d11d736573d1db5bfea7146792f4`

The first post-reconciliation family cycle returned `PASS_ACTIVE`, preserved zero merge/destructive/child-self-activation actions, and retained current CIE post-activation assurance.
