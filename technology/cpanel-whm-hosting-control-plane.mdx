---
title: "cPanel / WHM Hosting Control Plane"
sidebarTitle: "cPanel / WHM Control"
description: "Public-safe institutional registry for CrownThrive's cPanel/WHM hosting control plane, authenticated read adapter, Vault credential custody, account/capacity evidence and continuity boundaries."
icon: "server"
keywords: ["cPanel", "WHM", "hosting", "THIVEBASE", "Supabase Vault", "API", "infrastructure", "continuity"]
---

# cPanel / WHM Hosting Control Plane

## Institutional identity

```yaml
service_id: ct.service.hosting.whm
provider_family: cPanel_and_WHM
api_family: WHM_API_1
control_role: crownthrive_shared_web_hosting_control_plane
current_state: authenticated_read_verified
adapter_id: ct.adapter.cpanel-whm.api1.v1
adapter_runtime: deployed
adapter_auth: JWT_required
provider_mutations_enabled: false
credential_state: vaulted_dual_reference_verified
source_refs:
  - S111
  - S112
```

CrownThrive's cPanel/WHM estate is now an independently governed infrastructure service rather than an informal admin-panel dependency. Provider-account evidence, server observations, credential custody, application health, domain custody and platform lifecycle remain separate records.

## Verified current evidence

The August 21–22, 2026 certification established:

- a current cPanel/WHM administrator snapshot and provider version observation;
- **44 cPanel accounts** in the authenticated WHM account inventory;
- **8 accounts in suspended state** at the certification snapshot;
- authenticated WHM API 1 reads for account inventory, system load and server hostname;
- a server-side credential stored in Supabase Vault with a governed recovery reference and CHLOM credential-version custody;
- a deployed JWT-protected `cpanel-whm-api-control` adapter with an explicit read-only allowlist;
- request-response fingerprints and private control-plane evidence records;
- THIVEBASE provider and capacity records for infrastructure monitoring.

Exact account contacts, individual account rows, management endpoints, raw provider responses, credential values and other security-sensitive administration details remain outside public documentation.

## API boundary

The current adapter exposes only approved read operations:

| Operation | Risk | Current state |
| --- | --- | --- |
| Account inventory | D0 | authenticated read verified |
| System load | D0 | authenticated read verified |
| Server hostname | D0 | authenticated read verified |
| Disk usage | D0 | provider contract documented; bounded certification pending |

The provider-native token may possess capabilities beyond the adapter's allowlist. That does **not** authorize those capabilities institutionally. Provider-native ACL/privilege detail remains a separate verification item.

No account creation/deletion, suspension changes, package edits, DNS mutation, service restart, server configuration, API-token mutation or other provider write is enabled by the CrownThrive adapter.

## Credential custody

```yaml
credential_id: ct.credential.cpanel-whm.thrivebase
public_value: prohibited
primary_vault_reference: present
recovery_vault_reference: present
continuity_state: verified_dual_reference
version_history: CHLOM_managed
raw_secret_in_docs: prohibited
raw_secret_in_git: prohibited
raw_secret_in_logs: prohibited
```

The provider token label may be recorded for custody/recovery purposes, but the token itself is restricted runtime material. Public documentation records only opaque references and certification state.

## Hosting-account evidence rules

A cPanel account is **hosting evidence**, not a universal platform-state assertion:

- account present ≠ domain registered or controlled at the registrar;
- account unsuspended ≠ application healthy, reachable or production-certified;
- account suspended ≠ asset legally sunset or abandoned;
- account package/quota ≠ current application demand or commercial entitlement;
- hosting IP ≠ canonical DNS or TLS correctness;
- filesystem presence ≠ authoritative editable source repository;
- an API token ≠ blanket mutation authority.

The eight suspended accounts therefore enter lifecycle reconciliation. Some may correspond to intentional sunset/reserve assets; no automated unsuspension is authorized solely from the suspension flag.

## THIVEBASE integration

THIVEBASE now records the hosting provider as an infrastructure dependency with current account-count, suspension-count and load observations. Load averages are retained privately as time-stamped operational evidence. They are not interpreted as overload without an independently verified CPU/core and resource-capacity baseline.

This makes WHM evidence available to future capacity, continuity, deployment and platform-certification workflows while preventing provider telemetry from becoming a platform-status shortcut.

## Security and continuity controls

Current controls include:

- encrypted Vault storage and managed recovery reference;
- opaque credential version handle and rollback-capable custody history;
- JWT-required Edge Function adapter;
- read-only operation allowlist;
- response SHA-256 evidence rather than public raw payload replication;
- restricted hosting-control schema;
- provider-write gate closed;
- source/evidence separation between WHM, registrar, DNS/TLS, repositories and application runtime.

A post-migration security-advisor review produced no new finding specific to the private hosting-control schema. Existing unrelated informational RLS notices elsewhere in the CrownThrive database remain separate remediation work.

## Remaining certification gates

- verify provider-native token ACL/privilege scope and any expiration/revocation behavior without exposing the token;
- certify disk-usage read and establish server CPU/core, memory and storage capacity baselines before thresholding load;
- reconcile the eight suspended hosting accounts against the canonical asset lifecycle/disposition register;
- verify backup, restore, retention, account export and provider-exit procedures;
- map hosting accounts to canonical domains, DNS authority, TLS, mail and application deployments without conflating those systems of record;
- define monitoring cadence and alert thresholds before autonomous remediation;
- keep all WHM mutations fail-closed until an explicit action class, approval rule, rollback plan and independent verification exist.

## Downstream phase impact

- **Phase 3:** import the WHM provider, credential, endpoint, hosting-account and observation records into the executable infrastructure seed; keep provider mutation authority separate.
- **Phase 4:** platform certification may consume WHM hosting evidence but must still verify DNS/TLS, source/deployment and application health independently.
- **Phase 5:** customer-facing commerce or acquisition paths cannot rely on a hosting-account flag as availability evidence.
- **Phase 8:** Holdings/infrastructure reporting may use governed capacity and dependency evidence without treating cPanel accounts as additional portfolio assets.
- **Phase 10:** continuity requires credential rotation/revocation, off-provider backups/exports, hosting-account reconstruction, DNS/mail dependency mapping and a tested provider-exit path.

No Phase 2.99 or Phase 3 hard gate is relaxed by this integration.