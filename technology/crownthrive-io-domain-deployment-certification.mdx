---
title: "crownthrive.io Domain & Deployment Certification"
sidebarTitle: "Domain Deployment Certification"
description: "Current-main certification packet for CrownThrive hosting presence, custom-domain DNS/TLS readiness, ChatGPT Sites routing, provider-write authority and rollback evidence."
icon: "globe-lock"
tag: "HOLD"
keywords: ["cPanel", "DNS", "TLS", "ChatGPT Sites", "domains", "deployment", "rollback"]
---

# crownthrive.io Domain & Deployment Certification

## Continuity map

- [cPanel / WHM Hosting Control Plane](/technology/cpanel-whm-hosting-control-plane)
- [ThriveBase Hosting Access & FTPS Control](/technology/thrivebase-hosting-access-and-ftps-control)
- [Current-State Validation Queue](/knowledge/current-state-validation-queue)
- [Phase 3 Readiness Gate](/technology/phase-3-readiness-gate)

This packet reconciles the useful provider-presence evidence from stale PR #229 onto current `main` without treating cPanel presence as DNS, TLS or application certification.

## Current cPanel snapshot

```yaml
service_id: ct.service.cpanel.crownthrive-io
account_domain: crownthrive.io
provider_product_version: 134.0.51
captured_at: 2026-08-22T04:51:00Z
source_ref: S115
evidence_level: E2_owner_attested
domain_count: 12
inventory_complete: true
inventory_sha256: dfaf0f813ff5c92ff736448c0e142f042a5471b3a7cfea0a395985eebe765b04
```

The current hosting inventory contains one main domain, eight addon domains and three subdomains. Hosting presence proves account assignment only. Registrar custody, DNS authority, DNSSEC, TLS, mail, public application health, canonical routing and deployment identity remain independently typed states.

## Commercial ChatGPT Sites targets

| Target | Provider surface | Anonymous provider readback | Current target DNS | TLS / route |
| --- | --- | --- | --- | --- |
| `launch.crownthrive.com` | CrownThrive Launch ChatGPT Site | `401 Sign in required` | unresolved | HOLD |
| `ready.crownthrive.com` | CrownThrive Ready ChatGPT Site | `401 Sign in required` | unresolved | HOLD |
| `procure.crownthrive.com` | CrownThrive Procure ChatGPT Site | `401 Sign in required` | unresolved | HOLD |

No provider DNS record type or target value is published because current provider-authoritative custom-domain target evidence has not been obtained. An invented CNAME or A record would be an unsafe mutation.

## Provider-control boundary — current reconciliation

The earlier statement that the cPanel API read was failing is superseded. Current ThriveBase/hosting-control evidence establishes:

- authenticated crownthrive.io cPanel UAPI read via the JWT-protected account adapter, with `StatsBar/get_stats(diskusage)` certified;
- authenticated WHM allowlisted reads for account inventory, load and hostname;
- explicit-FTPS authentication, directory listing and safe hash-only file-read canary passed; the one-shot FTPS probe was then disabled;
- the current crownthrive.io cPanel domain inventory remains 12 rows;
- provider writes, arbitrary filesystem mutation, DNS mutation and shell execution remain closed.

Read certification does **not** provide a tested write rollback. Therefore this packet still authorizes **no DNS mutation, Force HTTPS mutation, cPanel write, FTPS write, domain creation, redirect change or TLS mutation**. The open gap is no longer basic provider read access; it is exact DNS authority/target evidence plus bounded mutation/readback/rollback certification where a future write is actually authorized.

## Required certification sequence

```text
provider-authoritative custom-domain target
→ DNS authority proof
→ bounded DNS write canary
→ provider/DNS read-after-write
→ rollback/absence proof
→ exact reapply
→ DNS propagation proof
→ TLS certificate proof
→ HTTPS and canonical-route readback
→ anonymous application readback
→ governed exact-digest acceptance
```

A custom-domain record must not be created until the exact provider target and the write/rollback authority are independently established.

## Relationship to commercial release

The Commercial Release Factory treats `dns_tls_route` and `governed_acceptance_publication` as separate hard gates. Historical ChatGPT Sites deployment evidence does not override the current anonymous `401` result or unresolved custom hostnames.

The three commercial sites remain credit-only and publication-held. No DNS or site-provider mutation occurred during this reconciliation.

## Current disposition

**HOLD — current readback and provider-write certification pending.**

This closes only the stale-branch reconciliation problem. It does not certify production domains, TLS, public access, checkout, entitlement, rights, tax, publication or Phase 3.