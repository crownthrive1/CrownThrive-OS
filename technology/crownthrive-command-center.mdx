---
title: "CrownThrive Command Center"
sidebarTitle: "Command Center"
description: "Provider-read production record for CrownThrive's public institutional portal, private governed control plane, and machine-readable proof surface."
icon: "server"
tag: "Production projection · scoped HOLD"
keywords: ["CrownThrive Command Center", "institutional control plane", "public proof", "Phase 0", "Phase 3", "D1"]
standard_version: "1.0.0"
primary_audience: "developer"
page_type: "reference"
content_state: "unresolved"
---

{/* pentadocs:audience-orientation:v2 */}
<Note>
  **Audience:** builders and integrators (`developer`).

  **This page:** CrownThrive Command Center — Provider-read production record for CrownThrive's public institutional portal, private governed control plane, and machine-readable proof surface.

  **Documentation state:** `unresolved` for page type `reference`. Documentation state is editorial posture only; it does not certify runtime, deployment, legal, rights, financial, provider, production, release, security, or operational status.

  **Unresolved reason:** no explicit page-level editorial acceptance state was declared in the migrated source.

  **Evidence needed:** an accepted governing source or accountable-owner attestation.

  **Review role:** PentaDocs governance.

  **Review trigger:** next material edit or source reconciliation.

  **Related guidance:** [Documentation source governance](/standards/documentation-source-of-truth-and-autonomous-governance) and [Institutional record standard](/standards/record-and-format-standard).
</Note>
{/* /pentadocs:audience-orientation:v2 */}

## CrownThrive Command Center

## Institutional status

The CrownThrive Command Center is a **functional UI designation**, not a new canonical trademark or permanent portfolio brand. Its platform identity remains a candidate until the required architecture decision and registry issuance are accepted.

| Field | Verified state |
| --- | --- |
| Public ecosystem rollout | Phase 0 — customer-facing consolidation and One Seat/SSO remain incomplete |
| Internal institutional controls | Phase 3 — Execute; this does not represent public feature completion |
| Application state | Existing public production projection operational at provider version 21 |
| Private control plane | Authorization negatives pass; production command execution and reusable connector bindings remain HOLD |
| Platform identity | Candidate; not yet institutionalized into the Sites Mesh |
| Documentation impact | Version-21 reconciliation candidate; exact-head security and release gates remain open |

## Hosted surface

The public application is available at [crownthrive-command-center.crownthrive.chatgpt.site](https://crownthrive-command-center.crownthrive.chatgpt.site).

Provider readback:

| Evidence | Exact value |
| --- | --- |
| Sites source SHA | `d94e1dc01348091ae479cd200b3fe2bb86958968` |
| CrownThrive-OS source at reconciliation | `90472fdce24d05a5f722b4029af4b466b43c3d4a` |
| Source namespace rule | Sites source and CrownThrive-OS Git source are distinct identities; neither is rewritten as the other |
| Sites version | `appgprj_6a8b7db920f48191bc57bf6f7ed4710b~appgver_5eae494bf5bc8191a8a4d8755081475b` (version 21) |
| Deployment | `appgdep_6a9651677cb08191894b81061fb00d03` |
| Provider state | `succeeded`; current production deployment preserved |
| Environment | revision `12` |
| Access readback | existing `public` policy revision `2`; no audience or editor expansion |
| D1 readback | 22 tables; zero `cos_sync_runs` and zero `cos_source_observations` |
| Public MCP | initialization and tool discovery respond; application-runtime write authority remains absent |

Prior failed and superseded deployments remain historical evidence and are not represented as the current release.

## What is implemented

### Public institution

The public surface includes the institutional home, ecosystem architecture, portfolio atlas, corridors, platforms, CIE, CHLOM, the canonical eight-stage Thrive Flywheel, products and services, licensing, developers, proof, status, changelog, insights, partnerships, and support.

The public projection is scope-limited. Unknown states remain unknown. Provider capability is never presented as CrownThrive operating truth.

### Private control plane

The `/command` route group includes ecosystem, platform, repository, release, documentation, integration, ThriveBase, commerce, growth, agent, governance, evidence, convergence, incident, recovery, and settings views.

Authentication uses Sites/ChatGPT identity dispatch. Authorization is server-resolved from explicit Founder, operator, and auditor allowlists. Missing allowlists fail closed. No frontend visibility rule is treated as institutional authorization.

The control plane has **no production executor** in this release. Natural-language classification is advisory; a write can only create a governed proposal envelope.

### Evidence and DAIL-compatible records

A staged proposal writes a minimized action record, a server-HMAC-pseudonymized actor reference, an audit event, and canonical `ct-canonical-json-v1` evidence to D1 in one batch. Raw intent is not persisted.

The provider-safe migration creates the evidence chain head and a unique prior-link guard. Before the first authorized staged write, the server must install four append-only triggers through prepared statements. Installation failure returns `NOT_STAGED`; no write proceeds. Records remain `UNSIGNED` until certified signing infrastructure exists.

### Machine interfaces

The application publishes:

- `/.well-known/crownthrive.json`
- versioned `/api/public/v1/*` resources
- OpenAPI and AsyncAPI contracts
- JSON Schemas
- `llms.txt`
- a bounded, stateless public MCP source route with read-only `search`, `fetch`, and dry-run `assess_governed_intent`
- sitemap, robots policy, RSS, canonical URLs, social metadata, and JSON-LD

The MCP source is deployed, but provider-native declaration and ChatGPT connection are not active. Source presence is not reported as a connected capability.

## Reconciled authority boundaries

- **CIE** is the Cultural Imprint Engine and governs cultural imprint and alignment.
- **CHLOM** governs broader authority, rights, permissions, licensing, provenance, evidence, and control.
- **PentaGreen** is the Commerce & Economic Activation Authority and resolves qualifying economic action through governed economic controls.
- CIE and CHLOM interoperate; they are not interchangeable.
- Provider success is evidence, not institutional economic truth.
- SoundCloud APIs remain `not_applicable` under the Founder override.

## Connection states

| System | Application-runtime state |
| --- | --- |
| Sites hosting | Connected; version 21 and exact Sites source read back |
| Sites D1 | Connected for proposal/evidence storage; no production executor |
| Sites identity and authorization | Private routes redirect unauthenticated callers; partner API returns `401`; reusable Founder command session remains unbound |
| Public MCP | Read-only initialization and tool discovery pass; no provider/customer mutation authority |
| GitHub | Work-session evidence connection only; application runtime `configuration_required` |
| PentaDocs | `crown-thrive` remains deployed from current `main`; branch hygiene repair is a governed candidate |
| ThriveBase-Private | 190 public-schema tables with RLS; security gate held on 21 `SECURITY DEFINER` views |
| ThriveBase-Public | 29 tables with RLS and zero current advisor findings; customer activation remains held |
| Stripe / Connect | Infrastructure exists; customer, Connect-webhook, entitlement, settlement, and payout activation remain separately gated |
| CIE | Controlled-test conditional pass; production certification `HOLD` |
| CHLOM / PentaGreen runtimes | Specified boundaries; activation not certified |
| External analytics, growth, email, and distribution runtimes | Not connected |

The candidate ThriveBase/Postgres migration packet is additive, RLS-first, includes validation and rollback, and was **not applied**.

## Activation holds

Founder proposal/evidence staging is configured but remains uncertified until all of the following read back successfully:

1. Every `SECURITY DEFINER` view receives a caller contract, least-privilege migration, negative tests, rollback, and advisor/API readback.
2. Sites command-plane execution receives a reusable production identity and independent authorization certification; Work-session connector access is never repurposed.
3. A bounded Founder proposal proves the D1 chain head, unique prior-link guard, append-only evidence, and server HMAC pseudonymization.
4. Developer-account, key, entitlement, usage, webhook, and source-provenance paths pass authenticated tenant-isolation and idempotency gates.
5. The exact source candidate passes the governed build, migration-ingestion, security, tenant-isolation, CIE manifest, stress, rollback, saved-version, and deployment-readback gates.

Public availability does not waive these private-control requirements.
