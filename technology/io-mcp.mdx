---
title: "CrownThrive IO and MCP"
sidebarTitle: "IO / MCP"
description: "The ecosystem interoperability, API gateway, tool-routing, metering, adapter, and agent-connectivity layer."
icon: "plug"
standard_version: "1.0.0"
primary_audience: "developer"
page_type: "reference"
content_state: "unresolved"
---

{/* pentadocs:audience-orientation:v2 */}
<Note>
  **Audience:** builders and integrators (`developer`).

  **This page:** CrownThrive IO and MCP — The ecosystem interoperability, API gateway, tool-routing, metering, adapter, and agent-connectivity layer.

  **Documentation state:** `unresolved` for page type `reference`. Documentation state is editorial posture only; it does not certify runtime, deployment, legal, rights, financial, provider, production, release, security, or operational status.

  **Unresolved reason:** no explicit page-level editorial acceptance state was declared in the migrated source.

  **Evidence needed:** an accepted governing source or accountable-owner attestation.

  **Review role:** PentaDocs governance.

  **Review trigger:** next material edit or source reconciliation.

  **Related guidance:** [Documentation source governance](/standards/documentation-source-of-truth-and-autonomous-governance) and [Institutional record standard](/standards/record-and-format-standard).
</Note>
{/* /pentadocs:audience-orientation:v2 */}

## CrownThrive IO and MCP

**CrownThrive IO** is the interoperability corridor. Its existing public role includes links, QR codes, biolinks, static sites, and lightweight digital-presence infrastructure. The institutional architecture expands that role into a governed **IO/MCP integration layer** that connects CrownThrive platforms, services, data contracts, and approved AI tools.

## Core responsibilities

The institutional layer should provide:

- platform and service adapters;
- stable API contracts;
- webhook intake and delivery;
- tool registration and capability discovery;
- identity and authorization propagation;
- event normalization;
- metering and usage records;
- rate limits and throttling;
- health/status reporting;
- secrets isolation;
- agent-facing MCP tools;
- public link, QR, vCard, event-link, and static-site services where applicable.

## MCP security model

Agents should never receive unrestricted vendor credentials or blanket database access. The gateway exposes narrowly scoped tools with explicit inputs, outputs, actor identity, permission checks, logging, and approval requirements.

```text
Agent / Platform
      ↓
CrownThrive ID
      ↓
IO / MCP Gateway
      ↓
CHLOM + Approval checks
      ↓
Scoped adapter / tool
      ↓
External or internal system
      ↓
DAIL event + CrownLytics signal
```

## Adapter doctrine

A third-party vendor can be replaced without changing institutional identity. For example, a link engine, ad server, community platform, publishing engine, or booking system may change while the CrownThrive service ID and business contract remain stable.

## Phase 3 requirement

The gateway should initially focus on a small set of high-value integrations rather than attempting to connect the entire portfolio at once. Each integration requires an owner, data contract, permissions, event taxonomy, failure behavior, backup/export route, and support runbook.
