---
title: CHLOM Wallet Continuity Factory & Automation v1
description: Deterministic continuity assets, exact-head automation, oracle reconciliation, recovery controls, prompts, agent contracts, ML advisory scoring, APIs, MCP tools, plugins and proprietary metaprotocol candidates.
---

# CHLOM Wallet Continuity Factory & Automation v1

This controlled-test layer turns continuity from an architectural intention into an institutional runtime contract.

It binds the existing CrownThrive proprietary factory, CHLOM Wallet execution envelopes, Policy Assurance, identity pins, DAIL lineage, recovery controls, modules, plugins, API/MCP surfaces and read-only oracle observations into one fail-closed continuity graph.

## Factory projection

The source catalog defines **24 continuity blueprints** projected across the **25 active proprietary-factory domains**, producing **600 deterministic candidate assets**. Projection is intentionally separate from factory generation advancement: the continuity suite is bound to generation 48 and may not claim generation 49 merely because new continuity projections exist.

Asset classes include pallets, modules, plugins, API contracts, MCP toolsets, code libraries, script runners, metaprotocol IP, prompt packs, agent contracts, ML feature pipelines, ML advisory models, oracle connectors, truth snapshots, heartbeat rules, identity pins, dependency graphs, schedulers, recovery plans, rollback scripts, observability probes, chaos scenarios, CI gates and documentation artifacts.

## Continuity fleet

The suite binds four existing Gen6 factory continuity/recovery identities by exact stable agent ID, DID and SHA-256 fingerprint:

- Assurance lane
- Culture lane
- Data lane
- Platform lane

A heartbeat label is not accepted as proof of freshness. Freshness is derived from the recorded heartbeat timestamp and deadline. An expired heartbeat automatically degrades continuity to HOLD; the continuity layer never manufactures a replacement heartbeat.

## Interfaces

The controlled-test package adds four Wallet continuity pallets, six modules, four internal plugin candidates, eight MCP contracts and one OpenAPI contract. It also binds the pre-existing `ct.chlom.backup-recovery` module and `ct.plugin.google-drive-continuity` provider-portability surface.

Discoverable MCP contracts remain non-actuating. They can read continuity state or append private evaluation evidence, but they cannot move money, grant Rights, access credentials, write to providers, broadcast to a chain, enable checkout, perform destructive recovery or advance a phase.

## Continuity algorithms

The source-controlled runtime implements:

- canonical hashing and deterministic asset generation;
- exact CrownThrive request-budget semantics (`-1` unlimited local ceiling, `0` zero requests, positive integers exact ceilings, `null` unresolved/fail closed);
- evidence-expiry evaluation;
- heartbeat freshness derivation;
- read-only oracle validation;
- strictest-wins ECAC/HOLD/DENY composition;
- deterministic continuity dependency evaluation;
- reversible recovery planning;
- a fixed-weight ML-compatible risk advisory pipeline.

The ML score is advisory only. It cannot issue a final ECAC/HOLD/DENY decision or upgrade a deterministic HOLD/DENY.

## Proprietary metaprotocol candidates

The research registry defines eight continuity IP candidates, including Evidence Expiry & Downgrade Protocol, Deterministic Continuity Graph, Oracle Integrity Reconciliation Protocol, Factory Continuity Projection Protocol, Recovery Without Authority Manufacture Protocol, Institutional Truth Closure Protocol, Continuity Risk Advisory Firewall and Provider-Portable Continuity Envelope.

The registry is an architectural/IP record, not a legal conclusion about patentability, trademark status or exclusive rights.

## Automation

ThriveBase registers ten private continuity jobs. One hourly `pg_cron` scheduler evaluates which lanes are due and performs internal, fail-closed reconciliation only. The scheduler may downgrade stale state and append receipts; it may not create independent review, refresh a heartbeat without evidence, make a provider write or activate production.

Continuity lanes cover heartbeat invalidation, exact-head reconciliation, evidence expiry, oracle reconciliation, dependency drift, truth snapshots, recovery drills, cultural continuity, deterministic factory projection and documentation synchronization.

## Institutional truth

Provider success is evidence, not institutional truth. A continuity truth snapshot closes only across the registered source head, private ThriveBase state, DAIL/evidence lineage, exact agent identity, heartbeat freshness, dependency state and oracle evidence. Missing or stale evidence results in HOLD; forbidden authority boundaries result in DENY.

## Security boundary

The private runtime uses RLS + FORCE RLS, no anon/authenticated table access, service-role-only reads and append-only evidence writes. Recovery is non-destructive by default and all live economic, Rights, provider and chain authority remains outside this tranche.

Current release state: **CONTROLLED_TEST / DRAFT_HOLD**.
