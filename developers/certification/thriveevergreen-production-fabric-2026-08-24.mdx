---
title: "ThriveEvergreen Production Automation Fabric — Certification 2026-08-24"
description: "Public-safe machine-read certification for the ThriveEvergreen Production Automation Fabric and its Virality Music integration contract."
---

# ThriveEvergreen Production Automation Fabric — Certification

**Certification date:** August 24, 2026  
**Exact source packet head:** `402ae2e2f35536adf0cca1979594067c8f7ed7e6`  
**Canonical engine:** `ct.agent.thriveevergreen-autonomous-publisher.v2`  
**Fabric control runtime digest:** `08a29ad781c5285b49003e30570fa999f36a8729b74785eec4649672c8dc1d9b`

This record certifies the public-safe runtime facts observed after materializing the ThriveEvergreen Production Automation Fabric. It does not expand economic authority, grant rights, authorize money movement, authorize D3, or bypass exact candidate gates.

## Canonical engine

Machine readback from the canonical v2 engine reports:

- `runtime_mode = production_write`
- `component_runtime_state = production_publisher_active`
- `production_effects_enabled = true`
- `publication_activation_state = ECAC`
- D3 human-reserved
- generalized external dispatch remains `HOLD_EXACT_MUTATION_CONTRACT`

The Fabric extends this engine. It does not create a competing engine.

## Materialized Fabric inventory

The production Fabric control endpoint returned the exact expected inventory:

| Asset family | Count |
| --- | ---: |
| bounded specialist agents | 8 |
| skill definitions | 12 |
| internal plugin packs | 5 |
| CHLOM P-01→P-12 commerce projections | 12 |
| deterministic policy algorithms | 10 |
| advisory ML models | 3 |

The CHLOM skill/pallet canary was reconciled to preserve the original twelve canonical CHLOM Wallet pallet candidates while separately counting the twelve ThriveEvergreen cross-pallet projections. The post-repair canary passed with:

- canonical pallets: 12
- ThriveEvergreen cross-pallet projections: 12
- verified handoffs: 19
- pending aliases: 2
- invalid authority/live boundaries: 0
- provider write: false
- money movement: false
- rights grant: false
- chain broadcast: false
- phase advancement: false

ThriveEvergreen remains a cross-pallet commerce/economic subprotocol and does not become P-13.

## Production control API

```text
https://tzajnzshmtzjenqulehq.supabase.co/functions/v1/thriveevergreen-fabric-control
```

Public-safe routes:

- `GET /` — Fabric status
- `GET /manifest` — public-safe Fabric inventory
- `GET /vm-manifest` — Virality Music convergence manifest

Internal D1 routes:

- `POST /price-envelope` — deterministic nonbinding price envelope
- `POST /anomaly-score` — advisory-only anomaly score

D1 routes require an internal service/gateway credential. The credential is Vault-bound and is not returned by the runtime.

## API/MCP canaries

- Fabric status: **HTTP 200**
- Fabric manifest: **HTTP 200**
- VM convergence manifest: **HTTP 200**
- unauthenticated price-envelope attempt: **HTTP 403**
- authenticated price-envelope canary: **HTTP 200**
- authenticated anomaly-score canary: **HTTP 200**
- raw secret export: **false**

The price-envelope canary returned an `ADVISORY_ENVELOPE`, not an effective price. The anomaly canary returned `ADVISORY_ONLY`, not ECAC/HOLD/DENY authority.

## MCP contracts

Five Fabric tools are enabled with closed top-level schemas:

- `thriveevergreen.fabric.status`
- `thriveevergreen.fabric.manifest`
- `thriveevergreen.fabric.price_envelope`
- `thriveevergreen.fabric.anomaly_score`
- `thriveevergreen.fabric.vm_manifest`

None is a provider-write or money-movement operation. The existing generalized `thriveevergreen.publish.dispatch` remains separately disabled.

## Automation fabric

The Fabric reuses existing production schedules instead of duplicating them:

- ten bounded ThriveEvergreen v2 publisher slots per hour;
- Crown Credits settlement/readback reconciliation every 10 minutes;
- Stripe stablecoin effective-capability reconciliation every 6 hours.

Two missing non-mutating evidence cycles were added:

- daily Fabric drift scan — `23 4 * * *` UTC;
- weekly advisory ML calibration/feature-contract review — `41 5 * * 1` UTC.

Both were executed immediately after deployment and returned **PASS** with:

- 8 agents;
- 12 skills;
- 5 plugins;
- 12 pallet projections;
- 10 algorithms;
- 3 advisory ML models;
- zero agent-authority violations;
- zero algorithm-contract violations;
- zero ML authority/source violations;
- provider write false;
- money movement false;
- model-weight mutation false.

Cycle receipts are append-only, RLS-enabled and deny anon/authenticated/service-role DML.

## Vault and custody binding

The Fabric has restricted Vault envelopes for:

- source/manifest binding;
- authority boundary;
- internal Fabric gateway authentication.

Source artifacts are bound through the institutional asset registry to the exact source head and require Drive + DAIL custody.

Five executable/restricted capabilities are additionally bound in the Vault capability registry:

- Fabric Edge runtime;
- deterministic price-envelope policy capability;
- advisory anomaly-score capability;
- daily/weekly Fabric cycle RPC;
- non-executable authority-policy reference.

All have body exposure disabled. D1/D2 capability records require independent verification and do not inherit D3.

## MetaMask origin reconciliation

Founder-supplied current dashboard state records these allowlisted origins:

- `https://vm.crownthrive.com`
- `https://kjvsermontoolkit.crownthrive.com`

The institutional state is recorded as `founder_attested_added`. The current connector cannot independently read the MetaMask dashboard allowlist, so this is not mislabeled as machine-provider readback.

The remaining MetaMask/Virality gates are:

- provider Wallet Services production-plan entitlement verification;
- native Virality Music frontend injection/readback through the separately held Sites writer.

## Virality Music convergence readback

The VM Fabric manifest currently reports:

- Virality public service: read-verified;
- Virality commerce backend: write-verified;
- `1 Virality Credit = 25 Crown Credits`;
- automatic historical balance rewrite: false;
- CHLOM Wallet: write-verified;
- MetaMask allowlist: founder-attested added;
- WalletConnect: configured, frontend pending;
- dedicated Agent Wallet: persistent runner pending, unattended value ceiling `0`;
- Stripe stablecoin: `PROVIDER_PROPAGATION_PENDING`;
- native Sites write: HOLD;
- ThriveEvergreen v2: production-write / ECAC;
- generalized ThriveEvergreen dispatch: HOLD.

## Source packet

The exact source packet consists of:

- `commerce/thriveevergreen-production-fabric.mdx`
- `developers/manifests/thriveevergreen-production-fabric.v1.json`
- `developers/contracts/thriveevergreen/production-fabric.contracts.v1.json`
- `developers/scripts/thriveevergreen/production-fabric-runtime.v1.ts`
- `developers/ml/thriveevergreen/advisory-models.v1.json`
- `developers/prompts/thriveevergreen-vm-sol-ultra-integration-prompt.md`

The Sol Ultra prompt is the governed implementation handoff for integrating the Fabric into the existing Virality Music frontend without destructive rebuilds or authority bypass.

## Remaining intentional gates

1. **Virality native Sites source mutation** — existing Sites-mesh HOLD remains.
2. **MetaMask Wallet Services production entitlement** — provider plan verification pending.
3. **Dedicated Trust Wallet Agent Wallet** — persistent runner and encrypted wallet not yet bound; unattended value ceiling remains zero.
4. **Stripe stablecoin customer-effective Checkout** — dashboard enablement is founder-attested, but latest live Checkout readback remains propagation-pending.
5. **Changelly v2 authenticated API** — matching RSA private key remains required.
6. **ThriveEvergreen generalized dispatch MCP** — remains disabled until exact mutation, idempotency, readback, rollback/compensation and independent-certification gates pass.

These holds are capability boundaries inside the production architecture. They do not negate the production status of the canonical ThriveEvergreen v2 engine or the read/evaluate Fabric control plane.
