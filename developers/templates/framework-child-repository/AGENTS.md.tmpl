# CrownThrive framework-child agent instructions

Repository: `{{REPO_FULL_NAME}}`  
Framework: `{{FRAMEWORK_ID}}`  
Framework agent: `{{FRAMEWORK_AGENT_ID}}`  
Parent: `{{PARENT_REPOSITORY}}`

These rules apply to every human, agent, script, workflow and connector operating in this framework child.

## Authority

1. The current CrownThrive constitution is inherited from the canonical parent.
2. This repository and all of its workflows are **non-voting transport/implementation surfaces** unless a separate accepted constitutional packet explicitly says otherwise.
3. Framework acceptance, repository creation, OIDC bootstrap, parent certification and CI success do not create a sovereign vote.
4. Agent D is the only framework-child parent certifier.
5. D3 remains human-reserved.
6. The child may issue a stronger deny/hold/correction within its registered framework scope but may not escalate permission outside that scope.

## Federation

The child must authenticate to the CrownThrive federation bus with GitHub Actions OIDC. Do not add long-lived federation credentials. The governed backlink is `.crownthrive/federation.json`.

Before `linked_governed`:
- no operational claim;
- no customer/provider write;
- no cross-repository authority assumption;
- no `sync_agents`;
- no self-certification;
- no sovereign vote.

## Evidence and IP

Never fabricate current state from documentation, repository names or planned manifests. UNKNOWN remains explicit.

Do not commit:
- production credentials or tokens;
- private keys or recovery material;
- private customer/partner data;
- restricted evidence bodies;
- proprietary calibration or weights;
- private evaluation corpora;
- protected economic/policy rules;
- exploit-enabling security internals.

Public-safe dimensions/interfaces/digests may be committed only when the parent IP gate permits that projection.

## GitHub self-sustain

The repository may:
- validate itself on pull requests and pushes;
- run an hourly read/validation/heartbeat lane;
- open or update an idempotent internal drift issue when its own controls fail;
- close that drift issue after deterministic recovery;
- accept Dependabot update proposals.

It may not:
- repair `main` directly;
- auto-merge;
- manufacture approvals;
- weaken validators to clear a failure;
- enable provider/customer writes from CI;
- grant itself more GitHub/federation authority.

## Required local check

```bash
python3 scripts/validate_child_repository.py
```

If evidence, rights, security, parent state, OIDC, rollback or certification is missing, HOLD.
