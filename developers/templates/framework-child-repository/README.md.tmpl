# {{CANONICAL_NAME}}

This repository is the governed CrownThrive framework child for `{{FRAMEWORK_ID}}`.

## Institutional status

- Parent control repository: `{{PARENT_REPOSITORY}}`
- Framework agent: `{{FRAMEWORK_AGENT_ID}}`
- Repository identity: `{{REPO_ID}}`
- Planned repository: `{{REPO_FULL_NAME}}`
- Parent packet PR: `{{PARENT_PR}}`
- Bootstrap permitted by the current fleet manifest: `{{BOOTSTRAP_ALLOWED}}`
- Sovereign vote eligibility: **false**
- D3 authority: **human-reserved**
- Operational state at scaffold creation: **false**

Repository existence, workflow success, OIDC identity, federation registration, parent certification, model output, or a framework score does **not** create a CrownThrive sovereign vote.

## Required activation chain

```text
physical GitHub repository
→ immutable repository ID
→ governed child contract
→ GitHub Actions OIDC bootstrap
→ inherited governance/security checks
→ Agent D parent certification
→ linked_governed federation state
→ controlled-test operation
```

Until that chain is complete, this repository remains a scaffold or controlled-test candidate.

## Protected algorithm boundary

Public-safe contracts may describe dimensions, thresholds, schemas, hard blocks, lifecycle and digests. CrownThrive proprietary calibration, weights, private evaluation corpora, defensive rules, restricted evidence, credentials and private economic/policy implementation remain in approved private runtime/Vault and must not be copied here merely to make a test pass.

## Development

Run:

```bash
python3 scripts/validate_child_repository.py
```

All material changes use reviewed pull requests and the parent CrownThrive governance process. This child cannot self-certify, self-activate, change quorum, remove Agent D, create D3 authority or waive an upstream phase/security/rights/legal/finance gate.
