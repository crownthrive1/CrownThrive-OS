---
title: "ChatGPT Sites Mesh API & MCP"
sidebarTitle: "Sites Mesh API & MCP"
description: "CrownThrive's governed API/MCP mesh for ChatGPT Sites, including cross-site routing, dynamic feeds, module and agent bindings, PentaGreen economic gates, and native-provider bootstrap boundaries."
icon: "network-wired"
keywords: ["ChatGPT Sites", "Sites Mesh", "MCP", "API", "ThriveBase", "PentaGreen", "CHLOM", "site automation"]
standard_version: "1.0.0"
primary_audience: "developer"
page_type: "developer"
content_state: "unresolved"
---

{/* pentadocs:audience-orientation:v2 */}
<Note>
  **Audience:** Sites integrators, MCP developers, fleet operators, and security reviewers. Start with the public read contract on this page, then apply the [Sites release workflow](/workflows/crownthrive-sites-release-and-agent-workflow) and [MCP/API governance standard](/standards/mcp-api-governance) before any privileged operation.

  **This page:** ChatGPT Sites Mesh API &amp; MCP — CrownThrive's governed API/MCP mesh for ChatGPT Sites, including cross-site routing, dynamic feeds, module and agent bindings, PentaGreen economic gates, and native-provider bootstrap boundaries.

  **Documentation state:** `unresolved` for page type `developer`. Documentation state is editorial posture only; it does not certify runtime, deployment, legal, rights, financial, provider, production, release, security, or operational status.

  **Unresolved reason:** no explicit page-level editorial acceptance state was declared in the migrated source.

  **Evidence needed:** an accepted governing source or accountable-owner attestation.

  **Review role:** PentaDocs governance.

  **Review trigger:** next material edit or source reconciliation.
</Note>
{/* /pentadocs:audience-orientation:v2 */}

## ChatGPT Sites Mesh API & MCP

CrownThrive operates a governed mesh for registered ChatGPT Sites surfaces. The mesh centralizes site identity, peer discovery, module/pallet/agent bindings, governed catalog feeds, non-authoritative telemetry, and privileged planning/control without treating provider access as institutional authority.

<Warning>
  The mesh does not create native ChatGPT Sites provider-write authority. Native source or layout changes remain project-scoped, exact-version, rollback, and read-after-write gated. Destructive provider deletes are not granted by this contract.
</Warning>

## Contract

| Field | Current value |
| --- | --- |
| Loader contract | `ct.site-mesh-loader.v1` |
| Interop contract | `ct.contract.sites-mesh.v1` |
| Plugin | `ct.plugin.sites-mesh` |
| MCP server | `ct.mcp.crownthrive-sites-mesh` |
| Fleet orchestrator | `ct.agent.sites-fleet-orchestrator` |
| Decision ceiling | D2 |
| Autonomy ceiling | A2 |
| Sovereign vote effect | None |
| D3 | Human-reserved |
| Economic activation authority | PentaGreen |

PentaGreen resolves economic activation through `ECAC | HOLD | DENY`. Provider delivery, payment, storefront, API, or fulfillment evidence cannot manufacture pricing, entitlement, rights, settlement, or economic authority.

## Quickstart: public reads

The documented public base URL is:

```text
https://tzajnzshmtzjenqulehq.supabase.co/functions/v1/crownthrive-sites-mesh
```

<CodeGroup>

```bash Health
curl "https://tzajnzshmtzjenqulehq.supabase.co/functions/v1/crownthrive-sites-mesh/health"
```

```bash Fleet
curl "https://tzajnzshmtzjenqulehq.supabase.co/functions/v1/crownthrive-sites-mesh/fleet"
```

```bash Surface manifest
curl "https://tzajnzshmtzjenqulehq.supabase.co/functions/v1/crownthrive-sites-mesh/manifest?surface_id=ct.surface.crownthrive-developer-marketplace.production"
```

</CodeGroup>

<ParamField query="surface_id" type="string" required>
  Stable registered Sites surface ID used by `/manifest`, `/bindings`, and `/peers`. Use an ID from the verified fleet inventory; do not substitute a project title or hostname.
</ParamField>

<Info>
  These requests exercise public reads only. This page does not reproduce the complete response schema or error envelope; consume only fields pinned by the current mesh contract and do not infer provider-write or economic authority from a successful response.
</Info>

## Native provider inventory correction

Native provider enumeration on 2026-08-23 found 10 CrownThrive-owned Sites projects, not six. Four additional projects were registered without replacing any existing stable identity. The mesh now contains 10 stable surfaces, 170 governed bindings, and 90 directed peer relationships; every manifest resolves nine peers dynamically.

| Stable surface ID | Expected provider access | Mesh manifest | Governed feed |
| --- | --- | --- | --- |
| `ct.surface.crownthrive-developer-marketplace.production` | Public | PASS | PASS |
| `ct.surface.crownthrive-launch.production` | Sign-in required | PASS | PASS |
| `ct.surface.crownthrive-ready.production` | Sign-in required | PASS | PASS |
| `ct.surface.crownthrive-procure.production` | Sign-in required | PASS | PASS |
| `ct.surface.kjv-visualized.production` | Public | PASS | PASS |
| `ct.surface.virality-music.production` | Public | PASS | PASS |
| `ct.surface.crownthrive-sites.production` | Sign-in required | PASS | PASS |
| `ct.surface.crownthrive-holdings.production` | Sign-in required | PASS | PASS |
| `ct.surface.crownthrive-investor-room.production` | Sign-in required | PASS | PASS |
| `ct.surface.adluxe-network.production` | Public | PASS | PASS |

A sign-in-gated root is not represented as an outage when the provider access policy intentionally requires sign-in. Native access settings were preserved. This tranche performed no site/project deletion and granted no site-delete capability.

Founder owner-level provider membership was read back on all 10 projects. The provider surface exposed no scoped non-human application/service-principal capability suitable for the fleet orchestrator, so that provider role remains `HOLD`; it is not represented as founder access.

## Public REST surfaces

Supported public reads:

```text
GET /health
GET /fleet
GET /manifest?surface_id=<stable-surface-id>
GET /bindings?surface_id=<stable-surface-id>
GET /peers?surface_id=<stable-surface-id>
POST /telemetry
```

`POST /telemetry` accepts public-safe operational observations only. Those observations are non-authoritative and have no authority effect.

<Tabs>
  <Tab title="Public discovery">
    `GET /health`, `GET /fleet`, and the surface-filtered manifest, bindings, and peers routes expose the public-safe mesh projection.
  </Tab>
  <Tab title="Public telemetry">
    `POST /telemetry` accepts public-safe operational observations only. The request schema is not reproduced on this page, so do not construct a telemetry payload by inference.
  </Tab>
  <Tab title="Privileged control">
    MCP/API control requires an accepted founder/super-admin session, governed server-side scoped principal, or another accepted backend identity. No public endpoint or browser credential satisfies that requirement.
  </Tab>
</Tabs>

## Privileged MCP/API control

Privileged requests require a legitimate founder/super-admin session, a governed server-side scoped principal, or another accepted backend identity. Browser code must never contain service-role credentials or provider secrets.

Current MCP target protocol: `2026-07-28`.

Available tools:

- `sites.list`
- `sites.get`
- `sites.mesh.get`
- `sites.bindings.list`
- `sites.peers.list`
- `sites.routes.list`
- `sites.install.plan`
- `sites.update.queue`
- `sites.binding.set`
- `sites.telemetry.list`

`sites.update.queue` creates an internal D0-D2 work request only. It does not itself mutate ChatGPT Sites. Deletion, money movement, payout/transfer, credential revocation, rights grants, and D3 actions cannot be queued through this tool.

## Per-site binding model

Each registered Sites surface has 17 governed binding families and nine peer links. The binding families include:

- Commercial Sites Control module;
- Sites Mesh Interoperability module;
- PentaGreen Site Economic Gate;
- CHLOM Institutional Capability Runtime;
- CHLOM policy pallet;
- Sites Fleet Orchestrator and existing release relay/publisher/readback workers;
- governed catalog feed;
- CrownThrive Services Stack;
- CHLOM API control;
- Sites Mesh API/MCP;
- CrownThrive Interoperability MCP; and
- PentaGreen economic authority.

The peer graph is data-driven. With 10 registered sites, each site receives nine peer links and the fleet contains 90 directed peer relationships.

## Routine automation after bootstrap

The intended source architecture is a one-time generic loader plus durable remote control:

```text
ChatGPT Site
  -> Sites Mesh manifest
  -> governed module/pallet/agent bindings
  -> governed catalog feed
  -> peer-site links
  -> CrownThrive Services Stack / CHLOM APIs
  -> PentaGreen economic gate where economic state is involved
  -> non-authoritative telemetry
```

After the generic loader is installed and read back on a site, routine governed product/catalog and compatible module-state updates can be delivered centrally without editing that site's source for every change. Native visual/layout/source changes still require a certified native provider-write route.

## Native Sites bootstrap readback

Four projects completed the required native provider cycle:

<Steps>
  <Step title="Freeze pre-state">
    Preserve the exact source and provider version before mutation.
  </Step>
  <Step title="Write and save the loader">
    Apply the approved public-safe generic loader to the bounded project.
  </Step>
  <Step title="Read back the provider version">
    Retrieve the newly saved provider version rather than trusting the write response alone.
  </Step>
  <Step title="Verify the complete predicate set">
    Check manifest, feed, contract, peer count, build, access state, and secret-scan results.
  </Step>
  <Step title="Prove rollback">
    Restore the frozen pre-state and verify the change is absent.
  </Step>
  <Step title="Reapply and verify final state">
    Reapply the exact approved loader and perform final readback.
  </Step>
</Steps>

Those completed provider cycles cover Developer Marketplace, Launch, Ready, and Procure. Their provider-cycle evidence passed, while institutional binding activation remains `HOLD` because an unsuperseded control-plane certification mismatch is still open.

Six projects remain native source-mutation `HOLD`:

- KJV Visualized / Sermon Toolkit: provider source checkout failed before mutation;
- Virality Music: the provider version advanced during the tranche and the local checkout contained concurrent work, so no overwrite was attempted;
- CrownThrive Sites, CrownThrive Holdings, CrownThrive Investor Room, and AdLuxe Network: institutionalized and centrally readable, but not natively mutated while the global control mismatch remains unresolved.

No source-bootstrap binding is represented as active without final native source readback.

## Current verification state

On 2026-08-23, all 10 registered surfaces returned HTTP 200 from the mesh manifest and governed catalog-feed routes. Each manifest returned 17 bindings, nine peers, the `ct.site-mesh-loader.v1` contract, ThriveEvergreen economic authority, and a prohibition on destructive provider deletion. The governed feed was empty and therefore created no active economic offer.

<Note>
  **Dated readback label.** **ThriveEvergreen** is retained in the preceding sentence because it records the value observed on 2026-08-23. [PentaGreen](/commerce/pentagreen) is the current reader-facing successor. The naming update does not rewrite that observed payload or prove that every source manifest has been migrated.
</Note>

The four completed provider cycles passed exact write/readback/rollback/reapply verification without access widening or secret exposure. The remaining six projects and all source-bootstrap binding activations remain explicitly held. Provider success is not represented as institutional authority.

## Security and authority boundary

The mesh is preservation-first and non-sovereign:

- no browser/service-role secret embedding;
- no self-approval;
- no silent deletion of evidence or source history;
- no direct money movement or final rights grant;
- no D3 automation;
- no native Sites project/source deletion authority;
- no direct-main write authority;
- service/agent principals do not impersonate the founder;
- independent readback is required after consequential writes; and
- public telemetry cannot manufacture authority.

## Errors, versions, and support

This page does not publish a complete REST error schema, telemetry payload, pagination contract, public rate policy, or MCP transport example. Do not invent retries or mutation behavior. Treat a missing/unknown surface, contract mismatch, incomplete provider readback, access-state drift, or unsatisfied control-plane predicate as a hold and preserve the last known safe state.

For loader or release issues, use the [Sites release workflow](/workflows/crownthrive-sites-release-and-agent-workflow). For authority, credential, or tool-boundary conflicts, use the [support request workflow](/workflows/support-request). The current MCP target protocol is `2026-07-28`; compatibility beyond the listed tool and contract families requires a separately pinned contract.
