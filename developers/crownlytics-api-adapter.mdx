---
title: "CrownLytics API Adapter"
sidebarTitle: "CrownLytics API"
description: "Governed CrownLytics REST API contract for websites, visitors, pageviews, goals, conversions, annotations, statistics and account data with fail-closed credential/runtime gates."
icon: "chart-line"
keywords: ["CrownLytics", "analytics API", "web analytics", "visitors", "pageviews", "goals", "conversions", "Bearer API", "CrownThrive"]
---

# CrownLytics API Adapter

<Warning>
  The CrownLytics API credential is restricted runtime-only material. It is never published in documentation, GitHub, examples, logs, screenshots, generated artifacts, email, or client-side code.
</Warning>

## Current institutional state

```yaml
platform_id: ct.platform.crownlytics
service_id: crownlytics
api_base_url: https://crownlytics.com/api
docs_url: https://crownlytics.com/api-documentation
architecture: REST
response_format: JSON
authentication: Bearer API key
timezone: UTC
credential_ref: vault:crownlytics_api_key
credential_vault_binding: blocked_pending_approved_secret_write
adapter: crownlytics-api-control
adapter_version: 1
adapter_auth: jwt_required
adapter_scaffold: passed
provider_dispatch: disabled
provider_writes: closed
authenticated_read: open
exact_endpoint_path_reconciliation: open
branding_context_reconciliation: open
phase_3_promotion: false
```

Current first-party CrownLytics documentation establishes REST/JSON, Bearer authentication, UTC semantics, the API base URL, and the current endpoint-family inventory. Founder-supplied account evidence corroborates current API access but does not replace a runtime test.

## Documented API families

- User
- Websites
- Pageviews
- Visitors
- Goals
- Goal conversions
- Annotations
- Website statistics
- Custom domains
- Account payments
- Account logs

## Public product role

CrownLytics is CrownThrive's privacy-first web analytics and measurement platform. Its current public positioning covers visitor journeys, real-time analytics, anonymized tracking, conversion measurement, heatmaps/session-replay capabilities, outbound-click monitoring, reports, exports, team collaboration and developer access.

Each advertised product feature still requires current implementation/provider/data-policy evidence before it becomes an API or production-capability claim in institutional automation.

## Source-context anomaly

The founder-supplied API excerpt contained a `locticians.com` header/context immediately before the CrownLytics API documentation. Current first-party CrownLytics documentation identifies `crownlytics.com` as the CrownLytics API surface.

```yaml
current_api_identity: CrownLytics
current_api_host: crownlytics.com
locticians_header_observation: preserved_source_context_anomaly
identity_merge: prohibited
reconciliation_state: open_nonblocking_context_check
```

Do not infer that CrownLytics is a Locticians API or that Locticians owns the analytics API from copied page chrome alone.

## Credential state

The founder supplied the current CrownLytics API key, but the connected secure-write layer rejected direct Vault insertion. The attempted write did not persist the value. Intended aliases:

```text
crownlytics_api_key
crownlytics_api_key_recovery
```

Authenticated provider dispatch remains disabled until an approved server-side secret-write path binds both aliases and continuity verification passes.

## Certification sequence

1. bind primary/recovery Vault aliases through an approved secret-write path;
2. extract exact paths/methods/query/body schemas for every documented family;
3. identify tenant/website ownership checks and data classes;
4. certify bounded read-only calls first;
5. validate output schemas, pagination, UTC timestamps, timeouts and request audits;
6. register only certified D0/D1 MCP tools with purpose-limited access;
7. keep mutations closed until separately authorized and reversible;
8. reconcile analytics retention, export, privacy and deletion controls with CrownThrive data policy.

## Institutional data boundary

CrownLytics may calculate analytics and expose provider records, but CrownThrive's governed claim/metric registry controls which figures become approved institutional or public claims. Raw analytics, session data and account logs are not automatically public evidence.

## Page changelog

- **2026-08-19 v1.0:** added current REST/Bearer/UTC/API-family evidence, fail-closed adapter and credential state, public product-role summary, and explicit preservation of the Locticians-header source-context anomaly without identity collapse.