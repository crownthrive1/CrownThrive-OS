{
  "name": "@crownthrive/os-control-plane",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "certify": "node --check api/chlom.js && node --check api/health.js && node --check api/mcp.js && node --check lib/chlom-fabric.js && npm test"
  }
}
