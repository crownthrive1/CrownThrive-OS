import { planGoals } from '../lib/cie-goals.js';
export default function handler(req,res){res.setHeader('Cache-Control','no-store, max-age=0');res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Methods','GET, POST, OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type');if(req.method==='OPTIONS')return res.status(204).end();if(req.method==='GET')return res.status(200).json({schema:'ct.cie.public-goal-plan.service.v1',status:'OPERATIONAL',purpose:'Map declared goals to routes, guardrail dependencies and evidence without executing providers.',method:'POST'});if(req.method!=='POST')return res.status(405).json({error:'method_not_allowed'});const body=typeof req.body==='object'?req.body:(()=>{try{return JSON.parse(req.body||'{}')}catch{return null}})();if(!body)return res.status(400).json({error:'invalid_json'});const plan=planGoals(body);return res.status(plan.goal_count?200:400).json(plan);}
